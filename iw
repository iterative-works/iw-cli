#!/usr/bin/env bash
# PURPOSE: Bootstrap script for iw-cli local development
# PURPOSE: Delegates to iw-run (dev layout matches release layout, no overrides needed)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "$SCRIPT_DIR/iw-run" "$@"
