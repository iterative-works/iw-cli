nodeLinker: node-modules
enableGlobalCache: false

npmScopes:
  "web.awesome.me":
    npmRegistryServer: "https://npm.cloudsmith.io/fortawesome/webawesome-pro/"
    npmAlwaysAuth: true
    npmAuthToken: "${WEBAWESOME_NPM_TOKEN}"
