# Code Review Results

**Review Context:** Phase 1: Domain Layer for issue IW-238 (Iteration 1/3)
**Files Reviewed:** 8
**Skills Applied:** code-review-style, code-review-scala3, code-review-testing, code-review-composition, code-review-architecture
**Timestamp:** 2026-03-07T22:42:47Z
**Git Context:** git diff 987e183

---

## Critical Issues

### 1. Test Package Mismatch
**Location:** All 4 test files (`.iw/core/test/*Test.scala`)
**Problem:** All test files declare `package iw.core.domain`, but the project convention is `package iw.tests` (see `IssueIdTest.scala`, `PhaseInfoTest.scala`). The `iw.core.domain` package doesn't exist anywhere in the codebase.
**Fix:** Change to `package iw.tests` in all 4 test files.

### 2. `markComplete` Idempotency Test Asserts Wrong Property
**Location:** `.iw/core/test/PhaseTaskFileTest.scala:24-27`
**Problem:** Test asserts newline count instead of content equality. Could pass for broken implementations.
**Fix:** Assert `assertEquals(result, content)` instead.

---

## Warnings

### 1. Unused Wildcard Import
**Location:** `.iw/core/test/PhaseOutputTest.scala:8`
**Problem:** `import ujson.*` is unused — all ujson references use explicit `ujson.` prefix.
**Fix:** Remove the import.

### 2. `PhaseOutput` Uses Raw `String` Instead of `PhaseNumber`
**Location:** `.iw/core/model/PhaseOutput.scala:11,26,44`
**Problem:** `phaseNumber: String` bypasses the type-safety of `PhaseNumber` opaque type.
**Impact:** Mild primitive obsession. Acceptable for now as these are output DTOs, but noted.

### 3. `toJson` Embeds Serialization in Model Layer
**Location:** `.iw/core/model/PhaseOutput.scala`
**Problem:** Mild FCIS violation — `ujson` is infrastructure-level but used in model.
**Impact:** Acceptable trade-off for simplicity given these are command output DTOs.

### 4. Duplicated `toJson` Structure
**Location:** `.iw/core/model/PhaseOutput.scala:15-60`
**Problem:** Identical serialization pattern copy-pasted 3 times.
**Fix:** Extract common `toJson(fields*)` helper.

### 5. `markComplete` Two-Pass Traversal
**Location:** `.iw/core/model/PhaseTaskFile.scala:19-29`
**Problem:** Scans lines twice (exists + map). Minor inefficiency.

### 6. `Integer.parseInt` Instead of Scala Idiomatic
**Location:** `.iw/core/model/PhaseBranch.scala:26`
**Problem:** Uses Java `Integer.parseInt` instead of Scala `.toInt`.

---

## Suggestions

- Add test for whitespace-padded input to `PhaseNumber.parse` (e.g., `" 1"`)
- Add test for multi-line `message` in `CommitOutput.toJson`
- Assert error message content in at least one `PhaseNumber` failure test
- Assert exact key sets in `PhaseOutput` JSON tests
- Consider using `PhaseNumber` type in `PhaseOutput` case classes (future improvement)
- Consider extracting `toJson` to output layer (future improvement if more output types added)

---

## Summary

- **Critical issues:** 2
- **Warnings:** 6
- **Suggestions:** 6
