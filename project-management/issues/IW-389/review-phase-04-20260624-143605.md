# Code Review Results

**Review Context:** Phase 4: Init + doctor integration + smoke/harness coverage for issue IW-389 (Iteration 1/3)
**Files Reviewed:** core/commands/Init.scala, core/commands/Doctor.scala, core/model/Config.scala, core/model/CIChecks.scala, core/test/CIChecksTest.scala, core/test/DoctorHarnessTest.scala, core/test/InitHarnessTest.scala, test/forgejo-issue.bats
**Skills Applied:** scala3, composition, architecture, testing, security, style
**Timestamp:** 2026-06-24 14:36:05
**Git Context:** git diff a4246660e397bad06df72a6cb6cd9fb2c3db7c22

---

## Summary

- Critical issues: 2 raised, 0 genuine blockers for this phase (both pre-existing or non-blocking — see triage).
- Warnings: ~9 (most are the same family-wide / deliberately-deferred items).
- Suggestions: several minor.

### Triage / disposition

**Raised as Critical — disposition:**

1. **architecture — `os.pwd` inside `CIChecks` model layer breaks FCIS purity.** Pre-existing
   (GitHub/GitLab arms already call `os.pwd`); the Forgejo arm copies the established pattern.
   Fixing requires threading a `cwd` param through the shared `checkWorkflowExistsWith`
   signature for ALL arms — out of scope for this phase, family-wide refactor. **Deferred**
   (own ticket, consistent with phases 1–3 deferral policy).
2. **testing — BATS smoke does not commit before branching.** The single current test (no-token
   error path) passes and never branches. Concern is future fragility. **Applied** the
   initial-commit + git-config hardening to match the `gitlab-issue.bats` convention.

**Applied (in-scope, strengthen this diff's new code):**
- style: `Init.scala` PURPOSE comment now lists Forgejo.
- testing/scala3: `InitHarnessTest` "forgejo without base-url" — removed stale unconsumed
  `"SAMP"` queued answer, added `askCallList.size == 1` assertion.
- testing: `InitHarnessTest` auto-detect + menu tests — added field assertions
  (repository / baseUrl / teamPrefix).
- testing: `CIChecksTest` — added "both `.forgejo` and `.github` present prefers `.forgejo`" case.
- architecture/scala3: `Doctor.scala` `ciPlatform` match made exhaustive
  (`Linear | YouTrack` explicit instead of wildcard `_`) for compile-time safety on future
  tracker additions.

**Deferred (deliberate scope decisions, per analysis RESOLVED clarifications):**
- Forgejo repository auto-detection from git remote — RESOLVED clarify scopes auto-detect to
  *tracker-type* on `codeberg.org` only; repository extraction is a non-goal this phase.
- Self-hosted Forgejo host heuristic in `TrackerDetector` — RESOLVED: auto-detect `codeberg.org`
  only; all other Forgejo instances selected explicitly.
- `baseUrl`/`repository` pre-write validation in Init — matches the sibling (YouTrack) pattern;
  validation runs at config read-time via `fromHocon`. Family-wide `baseUrl` scheme/SSRF +
  repository path sanitization already deferred to its own ticket in the Phase 2 review.

---

Full reviewer output retained in the workflow transcript. The actionable, in-scope subset was
applied; the remainder is deferred per the dispositions above.
