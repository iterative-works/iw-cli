# Code Review Results

**Review Context:** Phase 3: Capability wiring + command dispatch/auth for issue IW-389 (Iteration 1/3)
**Files Reviewed:** 6 (`core/commands/CommandEnv.scala`, `core/commands/Issue.scala`, `core/commands/LiveCommandEnv.scala`, `core/model/Constants.scala`, `core/test/IssueHarnessTest.scala`, `core/test/fixtures/FakeCommandEnv.scala`)
**Skills Applied:** code-review-scala3, code-review-composition, code-review-architecture, code-review-testing, code-review-security, code-review-style
**Timestamp:** 2026-06-24 13:58:13
**Git Context:** `git diff 177734c6e7881044b81b0c1e4b12fc376d824325`

---

## Summary

- **Critical issues:** 0
- **Warnings:** 11 (most pre-existing / family-wide; see disposition below)
- **Suggestions:** ~10

### Disposition

**Applied in this phase (in-scope, introduced by this diff):**
- Stale PURPOSE line in `Issue.scala` updated to include Forgejo (style).
- Added symmetric create-path coverage: `forgejo create without baseUrl` and `forgejo create without repository` harness tests, matching the fetch path's missing-config tests (testing).

**Deferred (pre-existing patterns / family-wide / out of this diff's scope):**
- `TrackerOps` trait per-tracker method growth — collapse to a single `fetchIssue`/`createIssue` pair with an `IssueTrackerConfig` ADT (architecture + composition). The Forgejo change correctly follows the established sibling pattern; refactoring the trait is a family-wide change affecting all five trackers, out of scope for a single-tracker phase. Own ticket.
- `ApiToken` opaque-type migration (scala3) — pre-existing model type, all trackers depend on it.
- `baseUrl` scheme validation / SSRF hardening and `repository` path sanitization (security) — live in the Phase-2 `ForgejoClient.scala` adapter, not in this diff; already flagged and deferred in the Phase-2 review as family-wide adapter conventions.
- Token value stored as plaintext in `FakeTracker` call `extras` (security) — pre-existing test-fixture pattern shared by all trackers; test-only.
- Token-resolution helper duplication (`linearToken`/`youtrackToken`/`forgejoToken`) and the duplicated config for-comprehension in `Issue.scala` (composition) — mirror the existing sibling pattern; extracting a generic helper is a cross-tracker cleanup.
- Stale trait-level Scaladoc on `TrackerOps` (architecture) and missing 2nd PURPOSE line in `IssueHarnessTest.scala` (style) — pre-existing wording not introduced by this diff.

---

## scala3

### Critical Issues
None.

### Warnings
- `ApiToken` is a `final case class` rather than an opaque type (`core/model/ApiToken.scala:10`). Pre-existing; each new tracker callsite deepens the investment. Deferred.

### Suggestions
- Token-resolution helpers (`linearToken`/`youtrackToken`/`forgejoToken`, `Issue.scala:99-121`) are structurally identical; could extract a generic `resolveToken(envVarName, env)`.
- `FetchIssueCall`/`CreateIssueCall` use stringly-typed `Map[String,String]` extras (`FakeCommandEnv.scala`); typed fields would be more refactor-safe. Pre-existing test infra.

---

## composition

### Critical Issues
None.

### Warnings
- `TrackerOps` grows a new per-tracker method pair with each tracker (`CommandEnv.scala:117-217`). Deferred (family-wide).
- Duplicated config for-comprehension in `Issue.scala` fetch and create Forgejo arms. Mirrors YouTrack precedent; deferred with the helper extraction above.

### Suggestions
- Extract `resolveToken` generic helper.
- `LiveTrackerOps` Forgejo delegations are trivial pass-throughs — context for the trait-design decision, no action.

---

## architecture

### Critical Issues
None.

### Warnings
- `TrackerOps` is becoming a tracker-specific method registry rather than a stable port (`CommandEnv.scala:117-217`). Recommend collapsing per-tracker issue methods behind a single pair with an `IssueTrackerConfig` sum type, matching how `createPullRequest`/`fetchCheckStatuses` already use a `ForgeType` discriminator. Deferred (family-wide; own ticket).
- Stale `TrackerOps` trait Scaladoc (`CommandEnv.scala:113-116`) still describes only PR/forge ops. Pre-existing wording; deferred.

### Suggestions
- `Issue.scala` file PURPOSE comment did not mention Forgejo. **Applied.**
- Parameter-ordering divergence between `fetchForgejoIssue` and `fetchYouTrackIssue` reflects genuinely different config shapes; resolved naturally by the unification above.

---

## testing

### Critical Issues
None.

### Warnings
- Missing test for Forgejo create without baseUrl (symmetric gap with fetch path). **Applied** (also added create-without-repository).
- "forgejo fetch without repository" relies on `SAMP-1` to bypass numeric-prefix resolution without an inline comment (`IssueHarnessTest.scala:305`). Minor; matches the existing GitHub test convention.
- "numeric arg composes team prefix" relies on `FakeTracker`'s default `Right` fetch result rather than an explicit `setFetchIssueResult`. Matches the existing GitHub test convention.

### Suggestions
- Long test name for "forgejo fetch without repository"; `forgejoConfig` fixture declared mid-file rather than with the other fixtures. Cosmetic.

---

## security

### Critical Issues
None.

### Warnings
- Token value stored in plaintext in `FakeTracker` call records (`FakeCommandEnv.scala:589`). Test-only, pre-existing pattern. Deferred.
- `baseUrl` accepted without scheme validation — SSRF surface (`ForgejoClient.scala:31,44,55`, Phase-2 adapter, not this diff). Deferred; recommend `http(s)://` validation + link-local rejection.

### Suggestions
- `repository` interpolated into the API URL without sanitization (`ForgejoClient.scala:32,45`). Phase-2 adapter. Validate `^[\w.-]+/[\w.-]+$`.
- `e.getMessage` in network-error mapping may leak connection details (`ForgejoClient.scala:167,211`). Phase-2 adapter.

---

## style

### Critical Issues
None.

### Warnings
- Stale PURPOSE line in `Issue.scala:2` did not list Forgejo. **Applied.**
- Missing 2nd PURPOSE line in `IssueHarnessTest.scala:1`. Pre-existing; deferred.
- New error-message lines may exceed the column limit (`Issue.scala`). Mirror the existing YouTrack strings already in the codebase; scalafmt format-check passes in pre-commit.

### Suggestions
- Verbose test name; cosmetic.
