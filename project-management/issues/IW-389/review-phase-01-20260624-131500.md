# Code Review Results

**Review Context:** Phase 1: Domain, config & serialization for issue IW-389 (Iteration 1/3)
**Files Reviewed:** 9 (core/model/Config.scala, Constants.scala, CIChecks.scala, RepoUrlBuilder.scala, TrackerUrlBuilder.scala; core/commands/Init.scala, Issue.scala; core/test/ConfigTest.scala, ConstantsTest.scala)
**Skills Applied:** code-review-scala3, code-review-architecture, code-review-testing, code-review-style
**Timestamp:** 2026-06-24 13:15:00
**Git Context:** git diff 8683556

---

## Critical Issues

### C1 — `teamIdentifier` omits Forgejo (Config.scala:145-149)
Forgejo falls into the `case _` wildcard and returns `team` (empty string for a Forgejo
config) instead of `repository`. Forgejo is repository-identified like GitHub/GitLab.
Silent runtime defect in the functional core.
**Fix:** add `IssueTrackerType.Forgejo` to the `GitHub | GitLab` arm.

### C2 — `RepoUrlBuilder` Forgejo arm builds a relative URL when baseUrl absent (RepoUrlBuilder.scala:34-36)
`config.trackerBaseUrl.getOrElse("")` yields `/$repo`; masked downstream by
`hasAllowedScheme` but inconsistent with `TrackerUrlBuilder`'s `for/yield`.
**Fix:** propagate `None` directly via `for/yield` (or `map`), mirroring TrackerUrlBuilder.

## Warnings (addressed)

### W1 — Temporal/planning names violate project naming rules
- Test names `"… (option A)"` (ConfigTest.scala ~1201, ~1222) encode a planning label.
- Stub messages `"… not yet implemented"` (Init.scala, Issue.scala) are temporal.
**Fix:** rename tests to describe behaviour; reword stubs to state the current constraint.

### W2 — Test coverage gaps (added)
- HOCON round-trip idempotence assertion (mirror GitLab test).
- Forgejo repository path-traversal rejection.
- `team =` absence negative assertion in toHocon test.

## Warnings (deferred to later phases — noted, not fixed in Phase 1)

### W3 — `CIChecks` groups Forgejo with Linear/YouTrack (CIChecks.scala:33-42)
Forgejo hosts its own Actions; a dedicated arm belongs with Phase 4 doctor CI-platform
detection. Deferred.

### W4 — `askForTracker` menu lacks Forgejo (Init.scala:108-121)
Interactive init for Forgejo is Phase 4 work; `collectTrackerDetails` stub is intentional.
Deferred.

## Suggestions (deferred)
- Encode config string in the enum (`enum IssueTrackerType(val configValue)`) to collapse
  three parallel matches — wider refactor, candidate for a follow-up.
- `TrackerDetector.suggestTracker` has no Forgejo auto-detection (Forgejo has no canonical
  host) — manual selection only; acceptable.

---

## Summary

- Critical issues: 2 (C1, C2) — fixed in iteration 2.
- Warnings: 4 — W1/W2 fixed; W3/W4 deferred to later phases by scope.
- Suggestions: 2 — deferred.
