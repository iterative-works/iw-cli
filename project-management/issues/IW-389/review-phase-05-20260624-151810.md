# Code Review Results

**Review Context:** Phase 5: PR creation + CI-check polling (forge parity) for issue IW-389 (Iteration 1/3)
**Files Reviewed:** 17 (11 source + 5 test + 1 BATS)
**Skills Applied:** architecture, scala3, composition, testing, security, style
**Timestamp:** 2026-06-24 15:18:10
**Git Context:** git diff d4e88442a6beebc8efc6020c22eab20d73020c4c

---

## Summary

- **Critical (correctness) issues: 0** — the feature compiles `-Werror` clean and all 186 unit/harness tests pass. No reviewer found a correctness bug.
- **High-value findings to fix this iteration:**
  1. *(testing)* `extractRepositoryFromPrUrl` and `fetchPrHeadSha` are on the Forgejo merge / CI-check path but have zero tests.
  2. *(testing)* `PhaseMergeHarnessTest` "fetchCheckStatuses" test asserts the merge call instead; `FakeTracker.fetchCheckStatuses` records neither `forge` nor `forgeConfig`, so forge routing to CI-check is unverified.
  3. *(architecture/composition/style — flagged by 4/6 reviewers)* `resolveForgeConfig` duplicated verbatim in `PhasePr`/`PhaseMerge`; identical Forgejo arms in `mergeSquashAndDelete`/`mergeWithDelete`; missing-config error strings duplicated 5–6×; `return`-based guard blocks.
  4. *(architecture)* pure URL parsers (`extractPullRequestIndex`, `extractRepositoryFromPrUrl`) live in the adapter — belong in `model/` per `core/CLAUDE.md`.
  5. *(testing)* missing success-status-code variants (200 for create, 204 for merge) and network-exception tests for merge/check.
  6. *(style)* `buildMergePullRequestBody()` → `val`; duplicate `cliTool`/`installUrl` tests; section-banner comments inconsistent with sibling adapters; PURPOSE line reads like a changelog.

- **Deferred (noted, not fixed this iteration):**
  - *(security)* config permits `http://` baseUrl → token sent in cleartext on misconfiguration. Low real risk (user's own instance, deliberate); follow-up: warn in config validation.
  - *(security)* sttp default backend follows redirects, forwarding `Authorization` header. Low risk for a configured dev instance; behavior change deferred.
  - *(architecture, suggestion)* `ForgeConfig` could be a sealed hierarchy making the Forgejo-valid case unrepresentable-when-empty — over-engineering for now.
  - *(composition, suggestion)* consolidate `gitlabHost` + `forgeConfig` into one `ForgeContext` when a third forge arrives.

See per-skill blocks below for full detail and file:line references.
