# Code Review Results

**Review Context:** Phase 3: E2E test adaptation for issue IW-344 (Iteration 1/3)
**Files Reviewed:** 10
**Skills Applied:** iterative-works:code-review-style, iterative-works:code-review-testing, iterative-works:code-review-security, iterative-works:code-review-scala3
**Timestamp:** 2026-04-17T20:20:44Z
**Git Context:** git diff 9627c6ac4d766b692642eb3c999072680a014e73

**Files:**
- .iw/commands/test.scala
- core/adapters/Process.scala
- test/core-jar.bats
- test/plugin-commands-describe.bats
- test/plugin-commands-execute.bats
- test/plugin-commands-list.bats
- test/plugin-discovery.bats
- test/project-commands-describe.bats
- test/project-commands-execute.bats
- test/project-commands-list.bats

---

<review skill="iterative-works:code-review-style">

## Code Style & Documentation Review

### Critical Issues
None.

### Warnings

- **test/core-jar.bats:99** — Test name `"build_core_jar overwrites existing jar without error (Phase-2 -f regression guard)"` references historical phase. Rename to describe observable behaviour.
- **test/core-jar.bats:33** — Teardown comment `# Clean up custom jar from scenario 4` couples to external task list. Describe what, not why-it-was-added.
- **core/adapters/Process.scala:2** — Second PURPOSE line only mentions `commandExists`; file now exposes `run`, `runStreaming`, `runInteractive`, `commandExists`.

### Suggestions

- **test/core-jar.bats:84** — Doubled `IW_CORE_JAR=` (prefix before `run` + inline in `bash -c`). Keep only the inline assignment.
- **Seven BATS setup files** — Identical 10-line `IW_CORE_JAR` + `touch -r` block duplicated. Candidate for `test/helpers/core-jar-setup.bash` extracted helper.

</review>

---

<review skill="iterative-works:code-review-testing">

## Testing Review

### Critical Issues

- **test/core-jar.bats:81 (IW_CORE_JAR override test)** — Uses hardcoded `/tmp/custom-iw-core.jar`. Not isolated to `$TEST_DIR`; parallel runs and teardown symlink attacks both possible. **Fix:** use `$TEST_DIR/custom-iw-core.jar` and drop the `/tmp` teardown line.
- **test/core-jar.bats:55-58 and :106-110 (`sleep 1` for mtime)** — Fragile wall-clock dependency to manufacture distinct mtimes. Reviewer suggests `touch -d "1 minute ago"` backdating of the jar instead.

### Warnings

- **.iw/commands/test.scala:149 and test/core-jar.bats:27** — Implicit interaction: parent exports shared jar path; `core-jar.bats` overrides per-test. Not covered by any assertion. Consider a comment in `test.scala` pointing at the contract.
- **Seven BATS setup files** — Duplicated 10-line block; extract into a `load`-able helper.
- **test/core-jar.bats:84** — Doubled `IW_CORE_JAR` assignment — pick one.
- **.iw/commands/test.scala:147** — Uses `return false` (imperative early exit) in an otherwise functional codebase.

### Suggestions

- **test/core-jar.bats:99** — "Phase-2 -f regression guard" is temporal; rename to describe the behaviour.
- **test/core-jar.bats:68-78 (fresh jar is silent)** — Asserts absence of rebuild marker but not that `version` produced valid output.

</review>

---

<review skill="iterative-works:code-review-security">

## Security Review

### Critical Issues
None.

### Warnings

- **test/core-jar.bats:34,81** — Hardcoded `/tmp/custom-iw-core.jar`. Shared world-writable path; teardown `rm -f` could follow a malicious symlink and parallel suites collide. Move inside `$TEST_DIR`.
- **core/adapters/Process.scala:86-102** — New `env` param forwarded unvalidated to `os.proc.call`. Low risk with current callers (all derived from `os.Path`), but `env` keys containing `=` or null bytes are not guarded. Consider `require` guard consistent with existing `SafeCommandPattern`.

### Suggestions

- **.iw/commands/test.scala:149** — After bootstrap succeeds, verify `os.exists(coreJar)` before injecting into child env; guards against launcher regressions silently writing the jar elsewhere.
- **test/core-jar.bats:42,50,60,75,84,94,113** — `bash -c '...'` pattern is `$TEST_DIR`-expansion-dependent; prefer direct `run "$TEST_DIR/iw-run" …` when possible.

</review>

---

<review skill="iterative-works:code-review-scala3">

## Scala 3 Idioms Review

### Critical Issues
None.

### Warnings
None.

### Suggestions

- **.iw/commands/test.scala:147** — `return false` early exit. Scala 3 idiom is a single expression with `if … then … else …` and no `return`.

</review>

---

## Summary

**Critical issues:** 2 (both from testing review; both in `test/core-jar.bats`).
**Warnings:** 8.
**Suggestions:** 6.

**Critical issue assessment:**

1. **`/tmp/custom-iw-core.jar` hardcoded path (testing + security reviews)** — Legitimate isolation/security concern, cheap to fix. Will be addressed in iteration 2.

2. **`sleep 1` for mtime differences (testing review)** — Pushing back: this is a common, correct idiom for mtime-granularity tests on filesystems with 1-second resolution. Tests pass reliably. The reviewer's alternative (`touch -d "1 minute ago"`) is a stylistic refinement, not a correctness fix. Not elevating to blocker; treating as a warning/suggestion.

**Plan:** Fix the `/tmp` path issue (critical per testing review, warning per security review). Defer `sleep 1`, duplicated-block extraction, PURPOSE comment polish, and test naming to follow-ups or live with them as acceptable.
