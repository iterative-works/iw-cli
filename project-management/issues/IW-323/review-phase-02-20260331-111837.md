# Code Review Results

**Review Context:** Phase 2: Plugin Discovery and Listing for issue IW-323 (Iteration 1/3)
**Files Reviewed:** 4 (iw-run, plugin-discovery.bats, plugin-commands-list.bats, plugin-commands-describe.bats)
**Skills Applied:** code-review-style, code-review-testing, code-review-security
**Timestamp:** 2026-03-31T11:18:37
**Git Context:** git diff fc0a2032b3bd3e4ad0d16ce6aec45e102121d1a1

---

## Style Review

### Warnings

1. **`local` with command substitution masks exit codes** — `local plugin_name=$(basename "$plugin_dir")` should be split into declaration and assignment to avoid masking failures under `set -e`.

2. **Hook-file filter logic duplicated verbatim** — The hook-skip pattern `[[ "$filename" =~ \.hook- ]]` appears 4+ times. Extract an `is_hook_file` predicate function.

3. **Two-pass scan for `has_plugin_commands`** — Listing loops scan twice (once to check, once to print). Could be simplified by buffering output.

4. **`plugin_cmd_name` verbose naming** — Inside the plugin branch of `describe_command`, a shorter name like `command_name` would be clearer.

### Suggestions

1. Test setup duplication across 3 BATS files — extract to shared helper via BATS `load`.
2. Inconsistent comments on `unset IW_PLUGIN_DIRS` across files.
3. `pname`/`pdir` abbreviations in `describe_command` — prefer `entry_name`/`entry_dir`.

---

## Testing Review

### Warnings

1. **`lib/` exclusion test doesn't test actual risk** — The `lib/` dir is a sibling to `commands/`, so the test trivially passes. Consider testing a non-command file inside `commands/` instead.

2. **Precedence test should assert section appears exactly once** — Add `grep -c` check for single plugin section.

3. **Setup duplication** — Same 50-line setup block across all three files. Extract to shared BATS helper.

### Suggestions

1. Add test for plugin with empty `commands/` directory (exists but no `.scala` files).
2. Add test for `--describe testplugin` (bare plugin name, no slash).
3. Add explanatory comments in error-path tests about setup dependencies.

---

## Security Review

### Warnings

1. **Colon delimiter in `discover_plugins` output** — Paths containing colons would break the `IFS=:` parsing. Low practical risk but fragile.

### Suggestions

1. Validate `plugin_name` against `^[a-zA-Z0-9_-]+$` in `discover_plugins()` itself.
2. Document trust boundary for plugin directories.
3. Consider stripping ANSI escape sequences from plugin header values.

---

## Summary

- **Critical Issues:** 0
- **Warnings:** 6
- **Suggestions:** 6

**Verdict:** Pass — no critical issues. Warnings are code quality improvements that can be addressed in this iteration.
