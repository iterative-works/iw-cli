# Code Review Results (Iteration 2)

**Review Context:** Phase 4: Command integration + dev mode for issue IW-345 (Iteration 2/3)
**Files Reviewed:** 30 (same scope as iter 1)
**Skills Applied:** iterative-works:code-review-style, iterative-works:code-review-testing, iterative-works:code-review-security, iterative-works:code-review-scala3, iterative-works:code-review-composition, iterative-works:code-review-architecture
**Timestamp:** 2026-04-23T16:03:25Z
**Git Context:** `git diff 3a19300e6d9c94f07b7907be8d7303646b0dfc0f`

## Iteration 1 critical fixes — confirmed resolved

1. **`./iw server start` path** — `iw-run` guard extended to `dashboard || server`. `ProcessManager.spawnServerProcess` now has `IW_DASHBOARD_JAR` set on every auto-start path.
2. **`ServerClientTest` silent skip** — `forkEnv` added to `dashboard.test` in `build.mill`; `assume(...)` removed; all 4 tests run and pass.
3. **`dashboard-dev-gate.bats` robustness** — `setup_file()` pre-builds jar; PID-tracked cleanup replaces broken `pkill`.

## Iteration 2 findings

### Critical issues
None that are Phase 4 regressions. The testing reviewer flagged three items — `CaskServerTest` in unit module, `ServerClientTest.isHealthy` starting a real server in unit scope, and `DashboardServiceTest` placeholder-no-assertion tests — all of which pre-exist Phase 4 and were only touched for signature updates. Treating these as out-of-scope follow-up.

### Warnings addressed in iteration 3
- **Scala 2 vararg splat** (`command: _*`) in `core/adapters/ProcessManager.scala:113` and `commands/dashboard.scala:225`. Use `cmd*`.
- **`AssetContext.isDevMode` uses `!=`** instead of pattern match on the enum. Switch to exhaustiveness-checked pattern match.
- **`mill_jar_path` path existence check** — currently checks non-empty only; add `.jar`-readable file check for clearer diagnostics.

### Warnings deferred (documented, not blocking)
- **`CaskServer.start` mixes pure resolution with startup side effects** (`Right(Off) if devMode && viteDevUrl.isEmpty` branch emits warning). Moving the warning into `ServerDaemon.main` would be cleaner but the current shape is correct and well-tested. Follow-up item.
- **`AssetContext.prod` shortcut** could be made `private[dashboard]`. No current misuse; cosmetic.
- **`hosts` comma-join lacks per-host validation** — inherited deferral from iteration 1.
- **`SampleDataCli` `statePath` lacks `..` check** — low severity (subprocess of `iw dashboard` run by the invoking user).
- **Comment polish** in `DevModeConfig.scala:48`, `CaskServerItest.scala` helper, `AssetUrl.scala` contract note — all within the "rationale is helpful" tolerance.

### Suggestions noted
- Encode "Off-with-warning" as an explicit `DevModeConfig` variant — defer.
- Move `DevModeConfig` pattern matching out of `AssetUrl` into `AssetContext.assetBaseUrl` — defer (structural; not a regression).
- `dashboard-rebuild-gate.bats` string-based Mill output assertions — fragile across Mill versions, acceptable for now.

## Summary

| Severity | Count | Action |
|---|---|---|
| Critical | **0** new Phase 4 regressions | — |
| Warnings | **9** (3 addressed in iter 3, 6 deferred) | Targeted iter-3 fix batch |
| Suggestions | 6 | Noted as follow-ups |

**Decision:** Apply the three quick, high-signal fixes inline as iteration 3, then proceed to commit. Out-of-scope testing cleanups (CaskServerTest placement, placeholder test removal) are tracked as follow-ups outside IW-345.
