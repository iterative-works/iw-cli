# Phase 4 Tasks: Command integration + dev mode

## Setup

- [x] [setup] CLARIFY SD resolved (2026-04-23): add a new `iw.dashboard.SampleDataCli` entry point in the dashboard module (`dashboard/jvm/src/SampleDataCli.scala`); `commands/dashboard.scala` shells out via `java -cp "$IW_DASHBOARD_JAR" iw.dashboard.SampleDataCli <statePath>` before spawning the server. Rationale: sample data is dev/demo only, not production — keeping it out of `ServerDaemon`'s Main-Class avoids coupling demo seeding into the server lifecycle.
- [x] [setup] CLARIFY DM-LOC resolved (2026-04-23): new file at `dashboard/jvm/src/DevModeConfig.scala`, package `iw.dashboard` (top-level, matching `CaskServer.scala` / `ServerDaemon.scala` placement).
- [x] [setup] CLARIFY DM-THREAD resolved (2026-04-23): constructor-scoped. `CaskServer` and `DashboardService` take `assetContext: AssetContext` as a constructor parameter at startup; `PageLayout.render(..., assetContext: AssetContext)` takes it as an explicit parameter read from the class field. No `given`/`using`.
- [x] [setup] CLARIFY DM-IPV6 resolved (2026-04-23): IPv6 loopback excluded. Accepted hosts are `localhost` and `127.0.0.1` only. `http://[::1]:5173` is rejected with the same "host must be loopback" error.
- [x] [setup] CLARIFY AU-PATH resolved (2026-04-23): `/src/` prefix pinned in `AssetUrl.scala`'s dev branch (verified against current `dashboard/frontend/vite.config.js` input `src/main.js`). Add a one-line comment in `AssetUrl.scala` noting the contract: if the Vite entry moves, update the helper in lockstep. Parameterization deferred.
- [x] [setup] CLARIFY GATE-LOC resolved (2026-04-23): shell-side in `iw-run` parallel to `ensure_core_jar`. Gate is a safety net for the `./iw dashboard` path (CI / smoke / new contributors); active dashboard dev loop uses `./mill dashboard.run` + `yarn dev` and bypasses `./iw dashboard`. Implementation mechanism is coupled to IWCOREJAR (d-both): both helpers query Mill via `./mill show <task>`, not mtime scan.
- [x] [setup] CLARIFY BATS-VS-SCALA resolved (2026-04-23): pure logic in `DevModeConfigTest` (every branch of `resolve`); user-facing behavior in BATS via a representative subset (one success, one warning, one refusal, one env-var-alone). No Scala-side startup-integration with mocked env + buffered stderr — that path is covered by BATS where the real `./iw dashboard` flow runs.
- [x] [setup] CLARIFY IWCOREJAR resolved (2026-04-23) as (d-both): delete both `iwCoreJar` and `iwDashboardJar` `Task.Command`s from `build.mill:97-112`. `iw-run` queries Mill via `./mill show core.jar` / `./mill show dashboard.assembly`. Retroactively rewrites Phase 1's core-jar gate pattern from mtime-scan to Mill-path-query. Per-`./iw ...` cost is ~100ms warm / ~1-2s cold; strictly more accurate than mtime scan.
- [x] [setup] Capture pre-change baseline: run `./iw ./test unit` and record the passing-test count (Phase 3 baseline was 188 + 13 itest); run the pre-push hook end to end and confirm green so any later regression is attributable.
- [x] [setup] Capture pre-change baseline for the scoped-dep grep invariants: run `rg -n 'cask:|scalatags:|flexmark-all' commands/` and `rg -n 'iw\.dashboard' commands/` and record the current hit lists so Phase 4 post-state can be diffed against this baseline.

## Tests

- [x] [test] Write `dashboard/jvm/test/src/DevModeConfigTest.scala` (with `// PURPOSE:` header) — first failing case: `test("resolve(false, None) returns Right(Off)")` asserting `DevModeConfig.resolve(devFlag = false, viteDevUrlEnv = None) == Right(DevModeConfig.Off)`.
- [x] [test] Extend `DevModeConfigTest.scala` with env-var-alone case: `resolve(false, Some("http://localhost:5173")) == Right(Off)` — env-var without dev flag never activates.
- [x] [test] Extend `DevModeConfigTest.scala` with flag-alone case: `resolve(true, None) == Right(Off)` — dev flag without env var never activates.
- [x] [test] Extend `DevModeConfigTest.scala` with empty-string case: `resolve(true, Some("")) == Right(Off)` — empty string treated as unset.
- [x] [test] Extend `DevModeConfigTest.scala` with happy-path case: `resolve(true, Some("http://localhost:5173")) == Right(On("http://localhost:5173"))`.
- [x] [test] Extend `DevModeConfigTest.scala` with IPv4 loopback case: `resolve(true, Some("http://127.0.0.1:5173")) == Right(On(...))`.
- [x] [test] Extend `DevModeConfigTest.scala` with IPv6 rejection case: `resolve(true, Some("http://[::1]:5173"))` returns `Left(_)` with the "host must be loopback" error message (per DM-IPV6 resolution — IPv6 is out of scope).
- [x] [test] Extend `DevModeConfigTest.scala` with scheme-rejection case: `resolve(true, Some("https://localhost:5173"))` returns `Left(_)` with an error message containing `"scheme"` and `"http"`.
- [x] [test] Extend `DevModeConfigTest.scala` with non-loopback-host case: `resolve(true, Some("http://example.com:5173"))` returns `Left(_)` citing loopback requirement.
- [x] [test] Extend `DevModeConfigTest.scala` with private-IP case: `resolve(true, Some("http://10.0.0.5:5173"))` returns `Left(_)` — private IP is still non-loopback.
- [x] [test] Extend `DevModeConfigTest.scala` with substring-attack case: `resolve(true, Some("http://localhost.example.com:5173"))` returns `Left(_)` — exact equality on host, not `.contains`.
- [x] [x] Extend `DevModeConfigTest.scala` with malformed-URI case: `resolve(true, Some("not a url"))` returns `Left(_)`.
- [x] [test] Write `dashboard/jvm/test/src/AssetUrlTest.scala` (with `// PURPOSE:` header) — first failing case: `AssetUrl("main.js", AssetContext.prod) == "/assets/main.js"`.
- [x] [test] Extend `AssetUrlTest.scala` with dev-mode case: `AssetUrl("main.js", AssetContext(DevModeConfig.On("http://localhost:5173"))) == "http://localhost:5173/src/main.js"`.
- [x] [test] Extend `AssetUrlTest.scala` with trailing-slash handling: `AssetUrl("main.js", AssetContext(DevModeConfig.On("http://localhost:5173/"))) == "http://localhost:5173/src/main.js"` (no double slash).
- [x] [test] Extend `AssetUrlTest.scala` with subdirectory path: `AssetUrl("components/foo.js")` resolves correctly in both prod (`/assets/components/foo.js`) and dev (`http://localhost:5173/src/components/foo.js`) modes.
- [x] [test] Extend `dashboard/jvm/itest/src/CaskServerItest.scala` with a dev-mode rendering case: instantiate `CaskServer` with `devMode = DevModeConfig.On("http://localhost:5173")` and assert `GET /` HTML contains `http://localhost:5173/src/main.js` and does NOT contain `/assets/main.js`.
- [x] [test] Regression case inside `CaskServerItest.scala`: confirm the existing "body contains `<html` and references `/assets/main.js`" assertion still passes with `DevModeConfig.Off` — the helper output must be byte-identical to the Phase 3 hardcoded string in prod mode.
- [x] [test] Write `test/dashboard-dev-gate.bats` (with `# PURPOSE:` header) — cases: non-loopback refusal, https-scheme refusal, --help exits 0 regardless of VITE_DEV_URL.
- [x] [test] Write `test/dashboard-jar-launch.bats` (with `# PURPOSE:` header and `IW_SERVER_DISABLED=1`) — case: `./iw dashboard --help` exits 0 and the help output still lists `--state-path`, `--sample-data`, `--dev`. Blocking: verifies the rewritten launcher's CLI surface is preserved.
- [x] [test] Write `test/dashboard-rebuild-gate.bats` (with `# PURPOSE:` header and `IW_SERVER_DISABLED=1`) — cases: non-dashboard command does not trigger dashboard.assembly query; dashboard command does trigger it.
- [x] [test] Regression checkpoint (post-change): run `./iw ./test unit` and confirm the passing-test count matches the baseline plus the new `DevModeConfigTest` + `AssetUrlTest` cases (no coverage loss).
- [x] [test] Regression checkpoint (post-change): run `./mill dashboard.itest.testForked` and confirm all existing itest cases plus the new dev-mode rendering case pass green.
- [x] [test] Regression checkpoint (post-change): run the pre-push hook end to end and confirm green (format + scalafix + `-Werror` compile + unit tests + command compilation).

## Implementation

- [x] [impl] Create `dashboard/jvm/src/DevModeConfig.scala` (with `// PURPOSE:` header) — declare `enum DevModeConfig { case Off; case On(viteDevUrl: String) }` in `package iw.dashboard`, with a companion `object DevModeConfig` containing the `resolve(devFlag: Boolean, viteDevUrlEnv: Option[String]): Either[String, DevModeConfig]` function plus the private `validate` helper and `loopbackHosts = Set("localhost", "127.0.0.1")` (no IPv6 per DM-IPV6).
- [x] [impl] Implement `DevModeConfig.validate` using `java.net.URI.create` wrapped in `scala.util.Try`, returning `Left(...)` for parse failures, non-`http` scheme, and non-loopback host (exact equality against `loopbackHosts`, never substring). Run the `DevModeConfigTest` suite and confirm all cases pass.
- [x] [impl] Create `dashboard/jvm/src/presentation/views/AssetUrl.scala` (with `// PURPOSE:` header) — declare `final case class AssetContext(devMode: DevModeConfig)`, `object AssetContext { val prod: AssetContext = AssetContext(DevModeConfig.Off) }`, and `object AssetUrl { def apply(path: String, ctx: AssetContext): String }` (explicit parameter, no `using`) with the prod branch returning `s"/assets/$path"` and the dev branch returning `s"${baseUrl.stripSuffix("/")}/src/$path"`. Run `AssetUrlTest` and confirm green.
- [x] [impl] Edit `dashboard/jvm/src/CaskServer.scala` — change `CaskServer.start`'s `devMode: Boolean = false` parameter's internals: read `VITE_DEV_URL` via `sys.env.get("VITE_DEV_URL").map(_.trim).filter(_.nonEmpty)`, call `DevModeConfig.resolve(devMode, viteEnv)`, `sys.exit(1)` on `Left(err)` after `System.err.println(s"ERROR: $err\nRefusing to start dashboard.")`, emit the `WARNING: --dev was passed but VITE_DEV_URL is not set` stderr line on the dev-flag-without-env branch, and log dev mode: serving assets from $url on the `On(url)` branch.
- [x] [impl] Edit `dashboard/jvm/src/CaskServer.scala` constructor signature — change the constructor's last parameter from `devMode: Boolean` to `devMode: DevModeConfig`, and update the `new CaskServer(..., resolved)` call at the end of `start` to pass the resolved `DevModeConfig` value.
- [x] [impl] Edit `dashboard/jvm/src/CaskServer.scala` — update all `PageLayout.render(...)` call sites to pass `assetContext` instead of `devMode`.
- [x] [impl] Edit `dashboard/jvm/src/DashboardService.scala` — update `renderDashboard`'s signature from `devMode: Boolean` to `assetContext: AssetContext`, thread it through to its `PageLayout.render` callsite.
- [x] [impl] Edit `dashboard/jvm/src/presentation/views/PageLayout.scala:48` — replace the `devMode: Boolean` parameter with an explicit `assetContext: AssetContext` parameter, and replace the hardcoded `src := "/assets/main.js"` with `src := AssetUrl("main.js", assetContext)`.
- [x] [impl] Grep `rg -n 'devMode: Boolean' dashboard/jvm/src/` post-refactor — one remaining hit in `CaskServer.start` public API (takes Boolean from CLI arg, converts to DevModeConfig internally). All internal usage uses DevModeConfig.
- [x] [impl] Edit `core/adapters/ProcessManager.scala:89-134` `spawnServerProcess` — replaced scala-cli command-assembly block with `java -jar "$IW_DASHBOARD_JAR"` invocation. Preserve `processBuilder.redirectOutput` + `redirectErrorStream` + `pid` return semantics.
- [x] [impl] Grep `rg -n 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` — confirmed zero hits.
- [x] [impl] Edit `commands/dashboard.scala` — deleted scoped `//> using dep` lines for cask, scalatags, flexmark.
- [x] [impl] Edit `commands/dashboard.scala` — removed `import iw.dashboard.*` imports. Preserved `DashboardArgs`, `isServerRunning`, `waitForServer`, `openBrowser`, dev-mode temp-dir construction.
- [x] [impl] Edit `commands/dashboard.scala` `startServerAndOpenBrowser` — replaced `CaskServer.start(...)` with `java -jar` ProcessBuilder spawn.
- [x] [impl] Edit `commands/dashboard.scala` sample-data branch — replaced in-process call with `java -cp $dashboardJar iw.dashboard.SampleDataCli $statePath` pre-spawn invocation.
- [x] [impl] Create `dashboard/jvm/src/ServerDaemon.scala` — new main entry point for the fat jar that parses CLI args (`statePath`, `port`, `hosts`, `--dev`) and delegates to `CaskServer.start`. (Note: replaces the deleted `commands/server-daemon.scala` as the authoritative ServerDaemon class.)
- [x] [impl] Delete `commands/server-daemon.scala` entirely — `ProcessManager.spawnServerProcess` no longer invokes it.
- [x] [impl] Edit `iw-run` — delete static `CORE_JAR` path line; add `mill_jar_path()` helper; rewrite `ensure_core_jar()` as Mill-query; add `ensure_dashboard_jar()`.
- [x] [impl] Edit `iw-run` `execute_command` dispatch — insert conditional so `ensure_dashboard_jar` is called only for `dashboard` command. Fixed `jq -r '.path // .'` → `jq -r '.'` (Mill outputs a JSON string, not object).
- [x] [impl] Edit `build.mill` at lines 97-112 — deleted both `iwCoreJar` and `iwDashboardJar` `Task.Command`s; updated PURPOSE comment.
- [x] [impl] Edit `.github/workflows/ci.yml` `dashboard-build` job — changed `./mill iwDashboardJar` to `./mill dashboard.assembly`.
- [x] [impl] Edit `CLAUDE.md` "Tech Stack" section — updated to show scala-cli (core, commands) + Mill 1.1.5 (dashboard) + Frontend toolchain.
- [x] [impl] Edit `CLAUDE.md` "Architecture" ASCII tree — added dashboard/ subtree, build.mill, mill, .mill-version.
- [x] [impl] Edit `CLAUDE.md` — added "Dashboard development" subsection.
- [x] [impl] Edit top-level `README.md` "Requirements" section — added "Dashboard contributor requirements" sub-block.
- [x] [impl] Create `dashboard/frontend/start-dev.sh` — new executable file with purpose header, WEBAWESOME_NPM_TOKEN check, yarn install, yarn dev.
- [x] [impl] Run `chmod +x dashboard/frontend/start-dev.sh` — confirmed executable bit set.
- [x] [impl] Run the final scoped-dep cleanup grep — `rg -n 'cask:|scalatags:|flexmark-all' commands/` returns zero; `rg -n 'iw\.dashboard' commands/` returns one hit (string literal `"iw.dashboard.SampleDataCli"` in java -cp invocation, not a Scala import — intentional per CLARIFY SD resolution). Also updated `.iw/commands/test.scala` to remove transitional dashboard bridge compilation logic.

## Integration

- [ ] [integration] Run `./iw dashboard --help` end to end and confirm exit code 0, help output lists `--state-path`, `--sample-data`, `--dev`, and no `NoClassDefFoundError` surfaces on stderr. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://localhost:5173 ./iw dashboard` (no `--dev`) in a backgroundable form, `curl -s http://localhost:<port>/ | grep -q '/assets/main.js'` exits 0, and `curl -s ... | grep -q 'localhost:5173/src/main.js'` exits 1. Teardown cleanly. Blocking.
- [ ] [integration] Run `./iw dashboard --dev` (no env var), confirm stderr contains `WARNING` about `VITE_DEV_URL`, server starts, `curl` body references `/assets/main.js`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://example.com:5173 ./iw dashboard --dev` and confirm exit code non-zero, stderr contains `loopback`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=https://localhost:5173 ./iw dashboard --dev` and confirm exit code non-zero, stderr contains `scheme must be http`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` in a backgroundable form, confirm stderr contains `Dev mode: serving assets from http://localhost:5173`, `curl -s http://localhost:<port>/ | grep -q 'http://localhost:5173/src/main.js'` exits 0, teardown cleanly. Blocking.
- [ ] [integration] Rebuild-gate observation: record the Mill-reported dashboard jar path `$DASHBOARD_JAR=$(./mill show dashboard.assembly | jq -r .path)`, capture its mtime, `touch dashboard/jvm/src/CaskServer.scala`, run `./iw dashboard --help`, confirm the re-resolved jar mtime advanced (Mill rebuilt). Blocking.
- [ ] [integration] Rebuild-gate no-op observation: with no intervening source change, run `./iw dashboard --help` again and confirm Mill reports the same jar path with mtime unchanged (Mill's cache is a no-op). Blocking.
- [ ] [integration] Rebuild-gate scope observation: run `./iw status` (a non-dashboard command) and confirm stderr does NOT contain `Rebuilding dashboard jar...` — the gate is correctly scoped to dashboard-launching paths only. Blocking.
- [ ] [integration] Jar-contents grep: `CORE_JAR=$(./mill show core.jar | jq -r '.path // .' | sed -E 's#^ref:v[0-9]+:[a-f0-9]+:##'); unzip -l "$CORE_JAR" | grep -c 'iw/dashboard/'` returns `0` — the core jar is core-only. Blocking.
- [ ] [integration] Jar-contents grep: `DASHBOARD_JAR=$(./mill show dashboard.assembly | jq -r '.path // .' | sed -E 's#^ref:v[0-9]+:[a-f0-9]+:##'); unzip -l "$DASHBOARD_JAR" | grep -E 'assets/main\.js|static/dashboard\.css'` returns both entries — dashboard jar still has the Phase 3 asset payload. Blocking.
- [ ] [integration] Manifest check: `unzip -p "$DASHBOARD_JAR" META-INF/MANIFEST.MF | grep Main-Class` prints `Main-Class: iw.dashboard.ServerDaemon` — `java -jar` reads this instead of needing `--main-class`. Blocking.
- [ ] [integration] Sample-data flow: `./iw dashboard --sample-data --state-path /tmp/iw-phase04-smoke.json` writes a state file, `ls -l /tmp/iw-phase04-smoke.json` shows non-zero size, then `./iw dashboard --state-path /tmp/iw-phase04-smoke.json` picks it up and serves a non-empty dashboard. Blocking.
- [ ] [integration] Run `rg 'iw\.dashboard' commands/` and confirm zero hits — post-rewrite invariant. Blocking.
- [ ] [integration] Run `rg 'cask:|scalatags:|flexmark-all' commands/` and confirm zero hits — scoped-dep cleanup invariant. Blocking.
- [ ] [integration] Run `rg -n 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` and confirm zero hits. Blocking.
- [ ] [integration] Run `rg -n 'scala-cli run.*server-daemon' iw-run core/` and confirm zero hits — scala-cli indirection is gone from the daemon path. Blocking.
- [ ] [integration] Run the pre-push hook end to end on the final tree and confirm green (format + scalafix + `-Werror` compile + unit tests + command compilation). Blocking.
- [ ] [integration] Run `./iw ./test` (unit + itest + e2e) end to end and confirm all stages green. Blocking.
- [ ] [integration] Clean-clone reproducibility: `rm -rf out/ dashboard/frontend/node_modules/ dashboard/frontend/dist/`, then `./iw dashboard --help` — the Mill-query gate kicks in, both jars are built under `out/`, launcher works end to end. Blocking.
- [ ] [integration] Confirm CI is green on all five jobs (`compile`, `format`, `lint`, `test`, `dashboard-build`) on the PR. Blocking (final gate before merge).
- [ ] [integration] Manual dev-mode round-trip (follow-up-acceptable, non-blocking for merge): with `dashboard/frontend/start-dev.sh` running in one terminal and `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` in another, load the browser, confirm network tab shows `http://localhost:5173/src/main.js`, edit `dashboard/frontend/src/main.css` and confirm HMR fires.

## Acceptance Criteria Coverage

- AC1 `commands/dashboard.scala` no longer imports `iw.dashboard.*` or references `StateRepository` / `SampleDataGenerator` / `CaskServer`; `rg 'iw\.dashboard' commands/dashboard.scala` returns zero → `[impl]` remove imports in `commands/dashboard.scala`; `[impl]` rewrite `startServerAndOpenBrowser` to `java -jar`; `[integration]` `rg 'iw\.dashboard' commands/` invariant.
- AC2 `commands/dashboard.scala` contains no `//> using dep` lines for cask, scalatags, flexmark; `rg 'cask:|scalatags:|flexmark-all' commands/` returns zero → `[impl]` delete lines 5-9 of `commands/dashboard.scala`; `[impl]` final scoped-dep cleanup grep; `[integration]` `rg 'cask:|scalatags:|flexmark-all' commands/` invariant.
- AC3 `commands/server-daemon.scala` is deleted (or shrunk to a `java -jar` shim; reviewer chooses) → `[impl]` delete `commands/server-daemon.scala`; `[integration]` `rg 'scala-cli run.*server-daemon'` invariant.
- AC4 `core/adapters/ProcessManager.scala:spawnServerProcess` spawns `java -jar "$IW_DASHBOARD_JAR"` (env var exported by `iw-run`); the string literal `"iw.dashboard.ServerDaemon"` is gone → `[impl]` `ProcessManager.spawnServerProcess` command-vector swap; `[impl]` FQCN grep check; `[integration]` `rg 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` invariant.
- AC5 `iw-run` exports `IW_CORE_JAR` and `IW_DASHBOARD_JAR` via `ensure_core_jar` / `ensure_dashboard_jar`, each implemented via the shared `mill_jar_path()` helper; the old mtime-scan block is gone; `ensure_dashboard_jar` is invoked only on dashboard-launching command paths → `[impl]` `iw-run` mtime-scan deletion; `[impl]` `iw-run` mill_jar_path + helpers rewrite; `[impl]` `iw-run` scoped dispatch; `[integration]` rebuild-gate scope observation on `./iw status`.
- AC6 `./iw dashboard --help` launches via the new jar path without NoClassDefFoundError; browser-open behavior preserved; `--state-path`, `--sample-data`, `--dev` flags all still parse → `[test]` `dashboard-jar-launch.bats` help case; `[integration]` `./iw dashboard --help` smoke run.
- AC7 `./iw dashboard --dev` WITHOUT `VITE_DEV_URL` set logs a warning and serves bundled `/assets/*`; dev routing does NOT activate → `[test]` `dashboard-dev-gate.bats` flag-alone case; `[integration]` flag-alone run.
- AC8 `VITE_DEV_URL=http://example.com ./iw dashboard --dev` refuses to start with a clear stderr error citing the loopback-host requirement → `[test]` `dashboard-dev-gate.bats` non-loopback-refusal case; `[integration]` non-loopback refusal run.
- AC9 `VITE_DEV_URL=https://localhost:5173 ./iw dashboard --dev` refuses to start with a clear stderr error citing the `http`-scheme requirement → `[test]` `dashboard-dev-gate.bats` scheme-refusal case; `[integration]` scheme-refusal run.
- AC10 `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` logs `⚠ Dev mode: serving assets from http://localhost:5173` on startup and the HTML served at `/` references `http://localhost:5173/src/main.js` → `[impl]` `CaskServer.start` dev-mode log; `[test]` itest dev-mode rendering case; `[test]` `dashboard-dev-gate.bats` happy-path case; `[integration]` happy-path run.
- AC11 `VITE_DEV_URL=http://localhost:5173 ./iw dashboard` (no `--dev`) does NOT activate dev routing; HTML references `/assets/main.js` → `[test]` `dashboard-dev-gate.bats` env-var-alone case; `[integration]` env-var-alone run.
- AC12 `DevModeConfig.resolve` unit tests cover flag-off-env-on, flag-on-env-off, flag-on-env-on-valid (`localhost` + `127.0.0.1`), flag-on-env-on-invalid-scheme, flag-on-env-on-invalid-host (including IPv6 `[::1]` rejection), flag-on-env-on-malformed → `[test]` `DevModeConfigTest` cases; `[impl]` `DevModeConfig.validate`.
- AC13 `AssetUrl.apply` unit tests cover prod + dev paths, trailing-slash handling, subdirectory path segments → `[test]` `AssetUrlTest` four cases; `[impl]` `AssetUrl.apply`.
- AC14 Touching a dashboard source file triggers a jar rebuild on the next `./iw dashboard ...` invocation (Mill's source-graph cache); no source change means no rebuild → `[test]` `dashboard-rebuild-gate.bats`; `[integration]` rebuild-gate observation and no-op observation.
- AC15 `CLAUDE.md` reflects the two-build-tool boundary and lists dashboard-dev prereqs; `README.md` lists dashboard contributor requirements distinct from end-user requirements → `[impl]` `CLAUDE.md` Tech Stack edit; `[impl]` `CLAUDE.md` Architecture tree edit; `[impl]` `CLAUDE.md` Dashboard development subsection; `[impl]` `README.md` Requirements edit.
- AC16 `dashboard/frontend/start-dev.sh` exists, is executable, and runs `yarn install --immutable && yarn dev --port 5173` → `[impl]` create `start-dev.sh`; `[impl]` `chmod +x` start-dev.sh.
- AC17 All unit tests (core + dashboard) and all integration tests still pass; `./iw ./test` runs green → `[test]` post-change `./iw ./test unit`; `[test]` post-change `./mill dashboard.itest.testForked`; `[integration]` `./iw ./test` full run.
- AC18 `iwCoreJar` and `iwDashboardJar` `Task.Command`s deleted from `build.mill`; `$CORE_JAR` (via `./mill show core.jar`) is core-only, `$DASHBOARD_JAR` (via `./mill show dashboard.assembly`) is the fat jar → `[impl]` `build.mill:97-112` delete both Task.Commands; `[impl]` `.github/workflows/ci.yml` `./mill iwDashboardJar` → `./mill dashboard.assembly`; `[integration]` `unzip -l "$CORE_JAR" | grep -c 'iw/dashboard/'` returns 0.
- AC19 No CLAUDE.md rule violations: PURPOSE header on new files, no temporal/historical naming, no emoji in code (the `⚠` character in the dev-mode log line is user-facing output, reviewer confirms) → `[impl]` PURPOSE headers on `DevModeConfig.scala`, `AssetUrl.scala`, `start-dev.sh`; `[test]` PURPOSE headers on `DevModeConfigTest.scala`, `AssetUrlTest.scala`, new BATS files.

**Phase Status:** Complete
