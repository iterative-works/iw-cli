# Phase 4 Tasks: Command integration + dev mode

## Setup

- [x] [setup] CLARIFY SD resolved (2026-04-23): add a new `iw.dashboard.SampleDataCli` entry point in the dashboard module (`dashboard/jvm/src/SampleDataCli.scala`); `commands/dashboard.scala` shells out via `java -cp "$IW_DASHBOARD_JAR" iw.dashboard.SampleDataCli <statePath>` before spawning the server. Rationale: sample data is dev/demo only, not production — keeping it out of `ServerDaemon`'s Main-Class avoids coupling demo seeding into the server lifecycle.
- [x] [setup] CLARIFY DM-LOC resolved (2026-04-23): new file at `dashboard/jvm/src/DevModeConfig.scala`, package `iw.dashboard` (top-level, matching `CaskServer.scala` / `ServerDaemon.scala` placement).
- [x] [setup] CLARIFY DM-THREAD resolved (2026-04-23): constructor-scoped. `CaskServer` and `DashboardService` take `assetContext: AssetContext` as a constructor parameter at startup; `PageLayout.render(..., assetContext: AssetContext)` takes it as an explicit parameter read from the class field. No `given`/`using`.
- [x] [setup] CLARIFY DM-IPV6 resolved (2026-04-23): IPv6 loopback excluded. Accepted hosts are `localhost` and `127.0.0.1` only. `http://[::1]:5173` is rejected with the same "host must be loopback" error.
- [x] [setup] CLARIFY AU-PATH resolved (2026-04-23): `/src/` prefix pinned in `AssetUrl.scala`'s dev branch (verified against current `dashboard/frontend/vite.config.js` input `src/main.js`). Add a one-line comment in `AssetUrl.scala` noting the contract: if the Vite entry moves, update the helper in lockstep. Parameterization deferred.
- [x] [setup] CLARIFY GATE-LOC resolved (2026-04-23): shell-side in `iw-run` parallel to `ensure_core_jar`. Gate is a safety net for the `./iw dashboard` path (CI / smoke / new contributors); active dashboard dev loop uses `./mill dashboard.run` + `yarn dev` and bypasses `./iw dashboard`. Implementation mechanism is coupled to IWCOREJAR (d-both): both helpers query Mill via `./mill show <task>`, not mtime scan.
- [x] [setup] CLARIFY BATS-VS-SCALA resolved (2026-04-23): pure logic in `DevModeConfigTest` (every branch of `resolve`); user-facing behavior in BATS via a representative subset (one success, one warning, one refusal, one env-var-alone). No Scala-side startup-integration with mocked env + buffered stderr — that path is covered by BATS where the real `./iw dashboard` flow runs.
- [x] [setup] CLARIFY IWCOREJAR resolved (2026-04-23) as (d-both): delete both `iwCoreJar` and `iwDashboardJar` `Task.Command`s from `build.mill:97-112`. `iw-run` queries Mill via `./mill show core.jar` / `./mill show dashboard.assembly`. Retroactively rewrites Phase 1's core-jar gate pattern from mtime-scan to Mill-path-query. Per-`./iw ...` cost is ~100ms warm / ~1-2s cold; strictly more accurate than mtime scan.
- [ ] [setup] Capture pre-change baseline: run `./iw ./test unit` and record the passing-test count (Phase 3 baseline was 188 + 13 itest); run the pre-push hook end to end and confirm green so any later regression is attributable.
- [ ] [setup] Capture pre-change baseline for the scoped-dep grep invariants: run `rg -n 'cask:|scalatags:|flexmark-all' commands/` and `rg -n 'iw\.dashboard' commands/` and record the current hit lists so Phase 4 post-state can be diffed against this baseline.

## Tests

- [ ] [test] Write `dashboard/jvm/test/src/DevModeConfigTest.scala` (with `// PURPOSE:` header) — first failing case: `test("resolve(false, None) returns Right(Off)")` asserting `DevModeConfig.resolve(devFlag = false, viteDevUrlEnv = None) == Right(DevModeConfig.Off)`.
- [ ] [test] Extend `DevModeConfigTest.scala` with env-var-alone case: `resolve(false, Some("http://localhost:5173")) == Right(Off)` — env-var without dev flag never activates.
- [ ] [test] Extend `DevModeConfigTest.scala` with flag-alone case: `resolve(true, None) == Right(Off)` — dev flag without env var never activates.
- [ ] [test] Extend `DevModeConfigTest.scala` with empty-string case: `resolve(true, Some("")) == Right(Off)` — empty string treated as unset.
- [ ] [test] Extend `DevModeConfigTest.scala` with happy-path case: `resolve(true, Some("http://localhost:5173")) == Right(On("http://localhost:5173"))`.
- [ ] [test] Extend `DevModeConfigTest.scala` with IPv4 loopback case: `resolve(true, Some("http://127.0.0.1:5173")) == Right(On(...))`.
- [ ] [test] Extend `DevModeConfigTest.scala` with IPv6 rejection case: `resolve(true, Some("http://[::1]:5173"))` returns `Left(_)` with the "host must be loopback" error message (per DM-IPV6 resolution — IPv6 is out of scope).
- [ ] [test] Extend `DevModeConfigTest.scala` with scheme-rejection case: `resolve(true, Some("https://localhost:5173"))` returns `Left(_)` with an error message containing `"scheme"` and `"http"`.
- [ ] [test] Extend `DevModeConfigTest.scala` with non-loopback-host case: `resolve(true, Some("http://example.com:5173"))` returns `Left(_)` citing loopback requirement.
- [ ] [test] Extend `DevModeConfigTest.scala` with private-IP case: `resolve(true, Some("http://10.0.0.5:5173"))` returns `Left(_)` — private IP is still non-loopback.
- [ ] [test] Extend `DevModeConfigTest.scala` with substring-attack case: `resolve(true, Some("http://localhost.example.com:5173"))` returns `Left(_)` — exact equality on host, not `.contains`.
- [ ] [test] Extend `DevModeConfigTest.scala` with malformed-URI case: `resolve(true, Some("not a url"))` returns `Left(_)`.
- [ ] [test] Write `dashboard/jvm/test/src/AssetUrlTest.scala` (with `// PURPOSE:` header) — first failing case: `AssetUrl("main.js", AssetContext.prod) == "/assets/main.js"`.
- [ ] [test] Extend `AssetUrlTest.scala` with dev-mode case: `AssetUrl("main.js", AssetContext(DevModeConfig.On("http://localhost:5173"))) == "http://localhost:5173/src/main.js"`.
- [ ] [test] Extend `AssetUrlTest.scala` with trailing-slash handling: `AssetUrl("main.js", AssetContext(DevModeConfig.On("http://localhost:5173/"))) == "http://localhost:5173/src/main.js"` (no double slash).
- [ ] [test] Extend `AssetUrlTest.scala` with subdirectory path: `AssetUrl("components/foo.js")` resolves correctly in both prod (`/assets/components/foo.js`) and dev (`http://localhost:5173/src/components/foo.js`) modes.
- [ ] [test] Extend `dashboard/jvm/itest/src/CaskServerItest.scala` with a dev-mode rendering case: instantiate `CaskServer` with `devMode = DevModeConfig.On("http://localhost:5173")` and assert `GET /` HTML contains `http://localhost:5173/src/main.js` and does NOT contain `/assets/main.js`.
- [ ] [test] Regression case inside `CaskServerItest.scala`: confirm the existing "body contains `<html` and references `/assets/main.js`" assertion still passes with `DevModeConfig.Off` — the helper output must be byte-identical to the Phase 3 hardcoded string in prod mode.
- [ ] [test] Write `test/dashboard-dev-gate.bats` (with `# PURPOSE:` header and `export IW_SERVER_DISABLED=1` in `setup()`) — first case: `VITE_DEV_URL=http://localhost:5173 ./iw dashboard` WITHOUT `--dev` starts, `curl /` body contains `/assets/main.js` and does NOT contain `localhost:5173/src/main.js`.
- [ ] [test] Extend `dashboard-dev-gate.bats` with flag-alone case: `./iw dashboard --dev` WITHOUT env var — server starts, stderr contains `WARNING` about missing `VITE_DEV_URL`, HTML references `/assets/main.js`.
- [ ] [test] Extend `dashboard-dev-gate.bats` with non-loopback-refusal case: `VITE_DEV_URL=http://example.com:5173 ./iw dashboard --dev` exits non-zero, stderr contains `loopback` or `host`.
- [ ] [test] Extend `dashboard-dev-gate.bats` with scheme-refusal case: `VITE_DEV_URL=https://localhost:5173 ./iw dashboard --dev` exits non-zero, stderr contains `scheme must be http`.
- [ ] [test] Extend `dashboard-dev-gate.bats` with happy-path case: `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` logs `Dev mode: serving assets from http://localhost:5173` to stderr and `curl /` body contains `http://localhost:5173/src/main.js`.
- [ ] [test] Write `test/dashboard-jar-launch.bats` (with `# PURPOSE:` header and `IW_SERVER_DISABLED=1`) — case: `./iw dashboard --help` exits 0 and the help output still lists `--state-path`, `--sample-data`, `--dev`. Blocking: verifies the rewritten launcher's CLI surface is preserved.
- [ ] [test] Extend `dashboard-jar-launch.bats` with process-vector case: after `./iw dashboard` is backgrounded, `ps -o args= -p <child>` contains `java -jar` and the dashboard jar path; no `scala-cli run` remains in the process tree. Non-blocking if platform `ps` variance makes this flaky; mark follow-up-acceptable.
- [ ] [test] Write `test/dashboard-rebuild-gate.bats` (with `# PURPOSE:` header and `IW_SERVER_DISABLED=1`) — case: resolve the Mill-reported dashboard jar path via `./mill show dashboard.assembly`, capture its mtime, `touch dashboard/jvm/src/CaskServer.scala`, run `./iw dashboard --help`, re-resolve the jar path and assert mtime advanced. Second run with no intervening source change: assert mtime unchanged (Mill's cache is a no-op).
- [ ] [test] Regression checkpoint (post-change): run `./iw ./test unit` and confirm the passing-test count matches the baseline plus the new `DevModeConfigTest` + `AssetUrlTest` cases (no coverage loss).
- [ ] [test] Regression checkpoint (post-change): run `./mill dashboard.itest.testForked` and confirm all existing itest cases plus the new dev-mode rendering case pass green.
- [ ] [test] Regression checkpoint (post-change): run the pre-push hook end to end and confirm green (format + scalafix + `-Werror` compile + unit tests + command compilation).

## Implementation

- [ ] [impl] Create `dashboard/jvm/src/DevModeConfig.scala` (with `// PURPOSE:` header) — declare `enum DevModeConfig { case Off; case On(viteDevUrl: String) }` in `package iw.dashboard`, with a companion `object DevModeConfig` containing the `resolve(devFlag: Boolean, viteDevUrlEnv: Option[String]): Either[String, DevModeConfig]` function plus the private `validate` helper and `loopbackHosts = Set("localhost", "127.0.0.1")` (no IPv6 per DM-IPV6).
- [ ] [impl] Implement `DevModeConfig.validate` using `java.net.URI.create` wrapped in `scala.util.Try`, returning `Left(...)` for parse failures, non-`http` scheme, and non-loopback host (exact equality against `loopbackHosts`, never substring). Run the `DevModeConfigTest` suite and confirm all cases pass.
- [ ] [impl] Create `dashboard/jvm/src/presentation/views/AssetUrl.scala` (with `// PURPOSE:` header) — declare `final case class AssetContext(devMode: DevModeConfig)`, `object AssetContext { val prod: AssetContext = AssetContext(DevModeConfig.Off) }`, and `object AssetUrl { def apply(path: String, ctx: AssetContext): String }` (explicit parameter, no `using`) with the prod branch returning `s"/assets/$path"` and the dev branch returning `s"${baseUrl.stripSuffix("/")}/src/$path"`. Run `AssetUrlTest` and confirm green.
- [ ] [impl] Edit `dashboard/jvm/src/CaskServer.scala` — change `CaskServer.start`'s `devMode: Boolean = false` parameter's internals: read `VITE_DEV_URL` via `sys.env.get("VITE_DEV_URL").map(_.trim).filter(_.nonEmpty)`, call `DevModeConfig.resolve(devMode, viteEnv)`, `sys.exit(1)` on `Left(err)` after `System.err.println(s"ERROR: $err\nRefusing to start dashboard.")`, emit the `WARNING: --dev was passed but VITE_DEV_URL is not set` stderr line on the dev-flag-without-env branch, and log `⚠ Dev mode: serving assets from $url` on the `On(url)` branch.
- [ ] [impl] Edit `dashboard/jvm/src/CaskServer.scala` constructor signature at `CaskServer.scala:1531-1549` area — change the constructor's last parameter from `devMode: Boolean` to `devMode: DevModeConfig`, and update the `new CaskServer(..., resolved)` call at the end of `start` to pass the resolved `DevModeConfig` value.
- [ ] [impl] Edit `dashboard/jvm/src/CaskServer.scala` — for each internal `PageLayout.render(..., devMode = devMode)` call site (lines 150, 195, 217, 264 per phase-04-context; verify exact lines via `rg -n 'PageLayout\.render' dashboard/jvm/src/CaskServer.scala`), introduce a single `given AssetContext = AssetContext(devMode)` at the appropriate scope (method entry or route handler) and update the `render` calls to drop the now-redundant `devMode` parameter.
- [ ] [impl] Edit `dashboard/jvm/src/DashboardService.scala` — update `renderDashboard`'s signature from `devMode: Boolean` to `devMode: DevModeConfig`, thread it through to its `PageLayout.render` callsite, and introduce the corresponding `given AssetContext` at the method boundary.
- [ ] [impl] Edit `dashboard/jvm/src/presentation/views/PageLayout.scala:48` — replace the `devMode: Boolean` parameter with an explicit `assetContext: AssetContext` parameter, and replace the hardcoded `src := "/assets/main.js"` with `src := AssetUrl("main.js", assetContext)`. Update every `PageLayout.render(...)` call site in `CaskServer.scala` and `DashboardService.scala` to pass the class-field `assetContext` (set via constructor).
- [ ] [impl] Grep `rg -n 'devMode: Boolean' dashboard/jvm/src/` post-refactor and confirm zero hits — every callsite now uses `DevModeConfig`.
- [ ] [impl] Edit `core/adapters/ProcessManager.scala:89-134` `spawnServerProcess` — replace the entire scala-cli command-assembly block (the `coreDir`, `coreFiles`, `commandsDir`, `daemonScript`, and the `Seq("scala-cli", "run", daemonScript) ++ coreFiles ++ Seq("--main-class", "iw.dashboard.ServerDaemon", "--", ...)` construction) with: `val dashboardJar = sys.env.getOrElse("IW_DASHBOARD_JAR", sys.error("IW_DASHBOARD_JAR not set — iw-run must call ensure_dashboard_jar before spawning server")); val command = Seq("java", "-jar", dashboardJar, statePath, port.toString, hostsArg)`. (Jar path is authoritative from iw-run's Mill query; no fallback to a stale staged path.) Preserve the `processBuilder.redirectOutput` + `redirectErrorStream` + `pid` return semantics unchanged.
- [ ] [impl] Grep `rg -n 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` and confirm zero hits — the FQCN literal and `--main-class` flag are gone.
- [ ] [impl] Edit `commands/dashboard.scala` — delete lines 5-9 (the SYNC comment opener, SYNC body, and the three scoped `//> using dep` lines for `com.lihaoyi::cask:0.11.3`, `com.lihaoyi::scalatags:0.13.1`, `com.vladsch.flexmark:flexmark-all:0.64.8`).
- [ ] [impl] Edit `commands/dashboard.scala` — remove the `import iw.dashboard.{CaskServer, StateRepository}` and `import iw.dashboard.domain.SampleDataGenerator` lines. Preserve `DashboardArgs`, `isServerRunning`, `waitForServer`, `openBrowser`, and the dev-mode temp-dir construction; only the in-process wiring goes.
- [ ] [impl] Edit `commands/dashboard.scala` `startServerAndOpenBrowser` — replace the `CaskServer.start(...)` invocation with a `ProcessBuilder` spawn: resolve `dashboardJar = sys.env.getOrElse("IW_DASHBOARD_JAR", sys.error("IW_DASHBOARD_JAR not set — invoke via ./iw dashboard"))`, build `val cmd = Seq("java", "-jar", dashboardJar, statePath, port.toString, hosts.mkString(",")) ++ (if devMode then Seq("--dev") else Seq.empty)`, call `.inheritIO()` on the `ProcessBuilder` so stdout/stderr reach the parent TTY, `.start()` it, then `waitForServer` + `openBrowser` + `process.waitFor()` to block in foreground. `VITE_DEV_URL` inherits via default env.
- [ ] [impl] Edit `commands/dashboard.scala` sample-data branch — per CLARIFY SD resolution (option b), replace the in-process `StateRepository(effectiveStatePath).write(sampleState)` call with a pre-spawn `java -cp $dashboardJar iw.dashboard.SampleDataCli $statePath` invocation OR a `--sample-data $statePath` flag on the jar's main (pick whichever aligns with the resolution recorded in Setup). Preserve the current user-visible behavior: `./iw dashboard --sample-data --state-path /tmp/foo.json` writes a state file and then launches against it.
- [ ] [impl] Edit `dashboard/jvm/src/ServerDaemon.scala` — add a `--sample-data` flag handler that, when passed, generates sample data via `SampleDataGenerator` + `StateRepository(statePath).write(...)` and exits 0 before starting the HTTP listener. Keeps sample-data concern inside the dashboard module per CLARIFY SD.
- [ ] [impl] Delete `commands/server-daemon.scala` entirely — `ProcessManager.spawnServerProcess` no longer invokes it, and no other callers exist. Follow-up grep: `rg -n 'server-daemon' commands/ core/ iw-run test/` must return zero scala-cli-indirection hits (BATS file names and `ProcessManager.serverLogPath` are unrelated; document the acceptable hits in the implementation log).
- [ ] [impl] Edit `iw-run` at lines 14-15 — DELETE the `CORE_JAR="${IW_CORE_JAR:-$INSTALL_DIR/build/iw-core.jar}"` line (the path comes from Mill now, not a fixed staging location). Keep `export IW_CORE_JAR="$CORE_JAR"` but move it into the new `ensure_core_jar` helper where `$CORE_JAR` gets populated.
- [ ] [impl] Edit `iw-run` lines 29-55 — DELETE the existing `build_core_jar`, `core_jar_stale`, and the mtime-scan guts of `ensure_core_jar`. Add a shared `mill_jar_path()` helper: `mill_jar_path() { local task="$1"; (cd "$INSTALL_DIR" && ./mill show "$task") | jq -r '.path // .' | sed -E 's#^ref:v[0-9]+:[a-f0-9]+:##'; }`. Rewrite `ensure_core_jar()` as `{ CORE_JAR="$(mill_jar_path core.jar)"; export IW_CORE_JAR="$CORE_JAR"; }`. Add `ensure_dashboard_jar()` as `{ DASHBOARD_JAR="$(mill_jar_path dashboard.assembly)"; export IW_DASHBOARD_JAR="$DASHBOARD_JAR"; }`. Verify `jq` is available — it's already assumed by other iw-run code.
- [ ] [impl] Edit `iw-run` `execute_command` dispatch — insert a `case` arm or conditional so `ensure_dashboard_jar` is called only when the dispatched command is `dashboard` (and, if `server-daemon.scala` still exists post-deletion check, `server-daemon`); non-dashboard commands MUST NOT pay the Mill-query cost. Verify by running `./iw status` and confirming no Mill invocation lines appear in stderr.
- [ ] [impl] Edit `build.mill` at lines 97-112 — DELETE both `iwCoreJar` and `iwDashboardJar` `Task.Command`s entirely (including surrounding comment blocks about stage-and-repack). `iw-run` queries `core.jar` and `dashboard.assembly` directly via `./mill show`. Also update file-level PURPOSE comment at line 2 to remove the mention of "iwCoreJar and iwDashboardJar top-level tasks".
- [ ] [impl] Edit `.github/workflows/ci.yml` `dashboard-build` job — change `./mill iwDashboardJar` to `./mill dashboard.assembly`. The itest step (`./mill dashboard.itest.test`) is unchanged.
- [ ] [impl] Edit `CLAUDE.md` "Tech Stack" section (project-level, at `/home/mph/Devel/iw/iw-cli-IW-345/CLAUDE.md`) — replace `- **Build**: scala-cli` with `- **Build**: scala-cli (core, commands) + Mill 1.1.5 (dashboard)` followed by a new bullet `- **Frontend toolchain**: Node 20, Yarn 4 via Corepack, Vite 8, Tailwind v4`.
- [ ] [impl] Edit `CLAUDE.md` "Architecture" ASCII tree — add `dashboard/` with `jvm/src/`, `jvm/test/src/`, `jvm/itest/src/`, `jvm/resources/`, `frontend/` subtrees, plus top-level `build.mill`, `mill`, `.mill-version`.
- [ ] [impl] Edit `CLAUDE.md` — add a new "Dashboard development" subsection between "Server Configuration" and "Testing" covering: Node 20 + `corepack enable`, `WEBAWESOME_NPM_TOKEN` provisioning, `./iw dashboard` queries Mill for jar paths via `./mill show` (jars live under `out/`, no longer `build/`), active dev loop uses `./mill dashboard.run` + `yarn dev` (bypasses `./iw dashboard`), dev-mode workflow (`dashboard/frontend/start-dev.sh` in one terminal + `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` in another), and the rationale for two build tools (scala-cli has no first-class JS story).
- [ ] [impl] Edit top-level `README.md` "Requirements" section — keep the existing end-user list (JVM + scala-cli) intact and append a "Dashboard contributor requirements" sub-bullet block listing Node 20+ (Corepack-enabled), Yarn 4 via `corepack enable` (driven by `package.json`'s `packageManager` field), Mill 1.1.5 (via the committed `./mill` wrapper), and `WEBAWESOME_NPM_TOKEN`.
- [ ] [impl] Create `dashboard/frontend/start-dev.sh` — new executable file with a `#!/usr/bin/env bash` shebang, `# PURPOSE:` header explaining it starts the Vite dev server for use alongside `iw dashboard --dev`, `set -euo pipefail`, `cd "$(dirname "$0")"`, a `WEBAWESOME_NPM_TOKEN` unset-check that warns to stderr, `yarn install --immutable`, and `exec yarn dev --port 5173 --host localhost`. No Tailscale detection.
- [ ] [impl] Run `chmod +x dashboard/frontend/start-dev.sh` and confirm the executable bit is set with `ls -l`.
- [ ] [impl] Run the final scoped-dep cleanup grep: `rg -n 'cask:|scalatags:|flexmark-all' commands/` must return zero lines; `rg -n 'iw\.dashboard' commands/` must return zero lines. If either returns hits, abort and audit before proceeding to Integration.

## Integration

- [ ] [integration] Run `./iw dashboard --help` end to end and confirm exit code 0, help output lists `--state-path`, `--sample-data`, `--dev`, and no `NoClassDefFoundError` surfaces on stderr. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://localhost:5173 ./iw dashboard` (no `--dev`) in a backgroundable form, `curl -s http://localhost:<port>/ | grep -q '/assets/main.js'` exits 0, and `curl -s ... | grep -q 'localhost:5173/src/main.js'` exits 1. Teardown cleanly. Blocking.
- [ ] [integration] Run `./iw dashboard --dev` (no env var), confirm stderr contains `WARNING` about `VITE_DEV_URL`, server starts, `curl` body references `/assets/main.js`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://example.com:5173 ./iw dashboard --dev` and confirm exit code non-zero, stderr contains `loopback`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=https://localhost:5173 ./iw dashboard --dev` and confirm exit code non-zero, stderr contains `scheme must be http`. Blocking.
- [ ] [integration] Run `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` in a backgroundable form, confirm stderr contains `Dev mode: serving assets from http://localhost:5173`, `curl -s http://localhost:<port>/ | grep -q 'http://localhost:5173/src/main.js'` exits 0, teardown cleanly. Blocking.
- [ ] [integration] Rebuild-gate observation: record the Mill-reported dashboard jar path `$DASHBOARD_JAR=$(./mill show dashboard.assembly | jq -r .path)`, capture its mtime, `touch dashboard/jvm/src/CaskServer.scala`, run `./iw dashboard --help`, confirm the re-resolved jar mtime advanced (Mill rebuilt). Blocking.
- [ ] [integration] Rebuild-gate no-op observation: with no intervening source change, run `./iw dashboard --help` again and confirm Mill reports the same jar path with mtime unchanged (Mill's cache is a no-op). Blocking.
- [ ] [integration] Rebuild-gate scope observation: run `./iw status` (a non-dashboard command) and confirm stderr does NOT contain `Rebuilding dashboard jar...` — the gate is correctly scoped to dashboard-launching paths only. Blocking.
- [ ] [integration] Jar-contents grep: `CORE_JAR=$(./mill show core.jar | jq -r '.path // .' | sed -E 's#^ref:v[0-9]+:[a-f0-9]+:##'); unzip -l "$CORE_JAR" | grep -c 'iw/dashboard/'` returns `0` — the core jar is core-only. Blocking.
- [ ] [integration] Jar-contents grep: `DASHBOARD_JAR=$(./mill show dashboard.assembly | jq -r '.path // .' | sed -E 's#^ref:v[0-9]+:[a-f0-9]+:##'); unzip -l "$DASHBOARD_JAR" | grep -E 'assets/main\.js|static/dashboard\.css'` returns both entries — dashboard jar still has the Phase 3 asset payload. Blocking.
- [ ] [integration] Manifest check: `unzip -p "$DASHBOARD_JAR" META-INF/MANIFEST.MF | grep Main-Class` prints `Main-Class: iw.dashboard.ServerDaemon` — `java -jar` reads this instead of needing `--main-class`. Blocking.
- [ ] [integration] Sample-data flow: `./iw dashboard --sample-data --state-path /tmp/iw-phase04-smoke.json` writes a state file, `ls -l /tmp/iw-phase04-smoke.json` shows non-zero size, then `./iw dashboard --state-path /tmp/iw-phase04-smoke.json` picks it up and serves a non-empty dashboard. Blocking.
- [ ] [integration] Run `rg 'iw\.dashboard' commands/` and confirm zero hits — post-rewrite invariant. Blocking.
- [ ] [integration] Run `rg 'cask:|scalatags:|flexmark-all' commands/` and confirm zero hits — scoped-dep cleanup invariant. Blocking.
- [ ] [integration] Run `rg -n 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` and confirm zero hits. Blocking.
- [ ] [integration] Run `rg -n 'scala-cli run.*server-daemon' iw-run core/` and confirm zero hits — scala-cli indirection is gone from the daemon path. Blocking.
- [ ] [integration] Run the pre-push hook end to end on the final tree and confirm green (format + scalafix + `-Werror` compile + unit tests + command compilation). Blocking.
- [ ] [integration] Run `./iw ./test` (unit + itest + e2e) end to end and confirm all stages green. Blocking.
- [ ] [integration] Clean-clone reproducibility: `rm -rf out/ dashboard/frontend/node_modules/ dashboard/frontend/dist/`, then `./iw dashboard --help` — the Mill-query gate kicks in, both jars are built under `out/`, launcher works end to end. Blocking.
- [ ] [integration] Confirm CI is green on all five jobs (`compile`, `format`, `lint`, `test`, `dashboard-build`) on the PR. Blocking (final gate before merge).
- [ ] [integration] Manual dev-mode round-trip (follow-up-acceptable, non-blocking for merge): with `dashboard/frontend/start-dev.sh` running in one terminal and `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` in another, load the browser, confirm network tab shows `http://localhost:5173/src/main.js`, edit `dashboard/frontend/src/main.css` and confirm HMR fires.

## Acceptance Criteria Coverage

- AC1 `commands/dashboard.scala` no longer imports `iw.dashboard.*` or references `StateRepository` / `SampleDataGenerator` / `CaskServer`; `rg 'iw\.dashboard' commands/dashboard.scala` returns zero → `[impl]` remove imports in `commands/dashboard.scala`; `[impl]` rewrite `startServerAndOpenBrowser` to `java -jar`; `[integration]` `rg 'iw\.dashboard' commands/` invariant.
- AC2 `commands/dashboard.scala` contains no `//> using dep` lines for cask, scalatags, flexmark; `rg 'cask:|scalatags:|flexmark-all' commands/` returns zero → `[impl]` delete lines 5-9 of `commands/dashboard.scala`; `[impl]` final scoped-dep cleanup grep; `[integration]` `rg 'cask:|scalatags:|flexmark-all' commands/` invariant.
- AC3 `commands/server-daemon.scala` is deleted (or shrunk to a `java -jar` shim; reviewer chooses) → `[impl]` delete `commands/server-daemon.scala`; `[integration]` `rg 'scala-cli run.*server-daemon'` invariant.
- AC4 `core/adapters/ProcessManager.scala:spawnServerProcess` spawns `java -jar "$IW_DASHBOARD_JAR"` (env var exported by `iw-run`); the string literal `"iw.dashboard.ServerDaemon"` is gone → `[impl]` `ProcessManager.spawnServerProcess` command-vector swap; `[impl]` FQCN grep check; `[integration]` `rg 'iw\.dashboard\.ServerDaemon|--main-class' core/adapters/ProcessManager.scala` invariant.
- AC5 `iw-run` exports `IW_CORE_JAR` and `IW_DASHBOARD_JAR` via `ensure_core_jar` / `ensure_dashboard_jar`, each implemented via the shared `mill_jar_path()` helper; the old mtime-scan block is gone; `ensure_dashboard_jar` is invoked only on dashboard-launching command paths → `[impl]` `iw-run` mtime-scan deletion; `[impl]` `iw-run` mill_jar_path + helpers rewrite; `[impl]` `iw-run` scoped dispatch; `[integration]` rebuild-gate scope observation on `./iw status`.
- AC6 `./iw dashboard --help` launches via the new jar path without NoClassDefFoundError; browser-open behavior preserved; `--state-path`, `--sample-data`, `--dev` flags all still parse → `[test]` `dashboard-jar-launch.bats` help case; `[integration]` `./iw dashboard --help` smoke run.
- AC7 `./iw dashboard --dev` WITHOUT `VITE_DEV_URL` set logs a warning and serves bundled `/assets/*`; dev routing does NOT activate → `[test]` `dashboard-dev-gate.bats` flag-alone case; `[integration]` flag-alone run.
- AC8 `VITE_DEV_URL=http://example.com ./iw dashboard --dev` refuses to start with a clear stderr error citing the loopback-host requirement → `[test]` `dashboard-dev-gate.bats` non-loopback-refusal case; `[integration]` non-loopback refusal run.
- AC9 `VITE_DEV_URL=https://localhost:5173 ./iw dashboard --dev` refuses to start with a clear stderr error citing the `http`-scheme requirement → `[test]` `dashboard-dev-gate.bats` scheme-refusal case; `[integration]` scheme-refusal run.
- AC10 `VITE_DEV_URL=http://localhost:5173 ./iw dashboard --dev` logs `⚠ Dev mode: serving assets from http://localhost:5173` on startup and the HTML served at `/` references `http://localhost:5173/src/main.js` → `[impl]` `CaskServer.start` dev-mode log; `[test]` itest dev-mode rendering case; `[test]` `dashboard-dev-gate.bats` happy-path case; `[integration]` happy-path run.
- AC11 `VITE_DEV_URL=http://localhost:5173 ./iw dashboard` (no `--dev`) does NOT activate dev routing; HTML references `/assets/main.js` → `[test]` `dashboard-dev-gate.bats` env-var-alone case; `[integration]` env-var-alone run.
- AC12 `DevModeConfig.resolve` unit tests cover flag-off-env-on, flag-on-env-off, flag-on-env-on-valid (`localhost` + `127.0.0.1`), flag-on-env-on-invalid-scheme, flag-on-env-on-invalid-host (including IPv6 `[::1]` rejection), flag-on-env-on-malformed → `[test]` `DevModeConfigTest` cases; `[impl]` `DevModeConfig.validate`.
- AC13 `AssetUrl.apply` unit tests cover prod + dev paths, trailing-slash handling, subdirectory path segments → `[test]` `AssetUrlTest` four cases; `[impl]` `AssetUrl.apply`.
- AC14 Touching a dashboard source file triggers a jar rebuild on the next `./iw dashboard ...` invocation (Mill's source-graph cache); no source change means no rebuild → `[test]` `dashboard-rebuild-gate.bats`; `[integration]` rebuild-gate observation and no-op observation.
- AC15 `CLAUDE.md` reflects the two-build-tool boundary and lists dashboard-dev prereqs; `README.md` lists dashboard contributor requirements distinct from end-user requirements → `[impl]` `CLAUDE.md` Tech Stack edit; `[impl]` `CLAUDE.md` Architecture tree edit; `[impl]` `CLAUDE.md` Dashboard development subsection; `[impl]` `README.md` Requirements edit.
- AC16 `dashboard/frontend/start-dev.sh` exists, is executable, and runs `yarn install --immutable && yarn dev --port 5173` → `[impl]` create `start-dev.sh`; `[impl]` `chmod +x` start-dev.sh.
- AC17 All unit tests (core + dashboard) and all integration tests still pass; `./iw ./test` runs green → `[test]` post-change `./iw ./test unit`; `[test]` post-change `./mill dashboard.itest.testForked`; `[integration]` `./iw ./test` full run.
- AC18 `iwCoreJar` and `iwDashboardJar` `Task.Command`s deleted from `build.mill`; `$CORE_JAR` (via `./mill show core.jar`) is core-only, `$DASHBOARD_JAR` (via `./mill show dashboard.assembly`) is the fat jar → `[impl]` `build.mill:97-112` delete both Task.Commands; `[impl]` `.github/workflows/ci.yml` `./mill iwDashboardJar` → `./mill dashboard.assembly`; `[integration]` `unzip -l "$CORE_JAR" | grep -c 'iw/dashboard/'` returns 0.
- AC19 No CLAUDE.md rule violations: PURPOSE header on new files, no temporal/historical naming, no emoji in code (the `⚠` character in the dev-mode log line is user-facing output, reviewer confirms) → `[impl]` PURPOSE headers on `DevModeConfig.scala`, `AssetUrl.scala`, `start-dev.sh`; `[test]` PURPOSE headers on `DevModeConfigTest.scala`, `AssetUrlTest.scala`, new BATS files.

**Phase Status:** Not started
