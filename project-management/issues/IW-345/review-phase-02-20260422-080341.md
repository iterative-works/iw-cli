# Code Review Results

**Review Context:** Phase 2: Dashboard JVM module for issue IW-345 (Iteration 1/3)
**Files Reviewed:** 8 (content-changed files only; ~90 mechanically relocated files excluded)
**Skills Applied:** code-review-style, code-review-testing, code-review-security, code-review-scala3
**Timestamp:** 2026-04-22T08:03:41Z
**Git Context:** git diff 4e34e8355ba37acf9f98427c18132a53c23e936a

---

<review skill="code-review-style">

## Code Style & Documentation Review

### Critical Issues
None found.

### Warnings

#### Comment in `build.mill` describes implementation mechanism, not purpose
**Location:** `build.mill:53-58`
The three inline comments in `iwCoreJar` (`// Unpack core classes`, `// Unpack dashboard classes on top`, `// Repack into the target jar`) narrate shell commands line-by-line rather than explaining why the manual jar-merging approach is used. Project rule: comment WHY, not WHAT.
**Recommendation:** Replace all three with one comment explaining why manual staging is needed (Mill has no built-in multi-module fat-jar task at this phase; transitional until Phase 4 introduces `dashboard.assembly`).

#### Comment in `.iw/commands/test.scala` is a "what" comment, not a "why"
**Location:** `.iw/commands/test.scala:100,106`
Two comments restate what the code already makes clear from variable names. No WHY.
**Recommendation:** Remove or replace with a rationale comment (transitional state until Phase 4).

### Suggestions
- Minor wording on `build.mill` PURPOSE header.
- `core/CLAUDE.md` dashboard/ section heading still says `**May import:** everything` — pre-existing, out of scope for Phase 2.

</review>

---

<review skill="code-review-testing">

## Testing Review

### Critical Issues

#### `StaticFilesTest`, `CaskServerTest`, `ServerClientTest` are integration tests placed in unit test module
**Location:** `dashboard/jvm/test/src/{StaticFilesTest,CaskServerTest,ServerClientTest}.scala`
These spin up real `CaskServer` instances on live TCP ports. They are integration tests by any definition. They now run under `./mill dashboard.test` which is invoked from the `unit` stage.

**Phase 2 disposition:** DEFERRED. Pre-existing classification issue — these tests already ran under `./iw ./test unit` via scala-cli's munit run against `core/test/` before this phase. Phase 2's explicit scope constraint (context "Out of scope": "Any refactoring of dashboard code beyond the mechanical rename") forbids reorganizing them. Flag for Phase 3 or a follow-up task.

### Warnings

#### Dashboard unit tests always run even when `testDir` guard fails (non-critical)
**Location:** `.iw/commands/test.scala:74-81`
If `core/test/` is empty, the function returns `true` early before reaching the dashboard call. Latent edge case, not a practical issue today.

#### `flexmark-all` declared twice — in `core` and `dashboard`
**Location:** `build.mill:23,38`
`dashboard` depends on `core` transitively. Declaring `flexmark-all` in both is misleading; will cause version-skew headaches in future.
**Recommendation:** Remove from `dashboard.mvnDeps`. **Applied in this iteration.**

#### TOCTOU port race in `CaskServerTest`, `StaticFilesTest` helpers
**Location:** `dashboard/jvm/test/src/{CaskServerTest,StaticFilesTest}.scala`
Pre-existing pattern in the migrated tests; not introduced by Phase 2. Flag for future hardening.

### Suggestions
- Munit version parity confirmed (`1.2.1` in both build.mill dashboard.test and core/project.scala).
- `TestFixtures.scala` correctly left in `core/test/` — no dashboard/jvm/test/src/ file imports it.

</review>

---

<review skill="code-review-security">

## Security Review

### Critical Issues
None found.

### Warnings

#### Version drift between bridge scripts and `build.mill`
**Location:** `commands/dashboard.scala:4-6`, `commands/server-daemon.scala:4-6`, `build.mill:32-38`
The three scoped `//> using dep` lines pin versions that must match `dashboard.mvnDeps`. Today they match but there's no mechanical link.
**Recommendation:** Add `// KEEP IN SYNC with dashboard.mvnDeps in build.mill` comment to both scripts. **Applied in this iteration.**

#### `dashboard.jar` classes silently overwrite `core.jar` classes during `iwCoreJar`
**Location:** `build.mill:46-52`
The stage-then-unpack sequence has no collision check. Low probability but soft assumption on stable Mill output paths.

**Phase 2 disposition:** DEFERRED with note. Collision would require a class-name collision between `iw.core.*` and `iw.dashboard.*`, which is structurally impossible given the package prefixes. Adding a warn-on-overwrite check would add complexity for near-zero practical benefit. Flag for Phase 4 when `iwCoreJar` may be replaced by `dashboard.assembly`.

### Suggestions
- `ProcessManager.scala:115` FQCN change is a proper `--main-class` positional argument — no injection surface.
- Security surface otherwise unchanged: no new endpoints, user-input handling, auth, or secrets.

</review>

---

<review skill="code-review-scala3">

## Scala 3 Idioms Review

### Critical Issues
None found.

### Warnings
None found.

### Suggestions
- `build.mill`: `scalaVersion` repeated in `core` and `dashboard` — two-module build, acceptable.
- `.iw/commands/test.scala`: dashboard unit tests always run — see testing review.
- No Scala 2 regressions introduced.

</review>

---

## Summary

- **Critical issues: 1** — deferred (pre-existing classification of integration tests; out of scope per phase context).
- **Warnings: 7** — 2 applied (comment improvements, flexmark dedup, KEEP IN SYNC comment); 5 deferred (pre-existing TOCTOU, dashboard-test-always-runs edge case, dashboard.jar overwrite guard).
- **Suggestions: 6** — noted; no action required for this phase.

Code review passes. Applied fixes are mechanical/low-risk; no changes to phase scope.
