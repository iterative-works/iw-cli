# Code Review Results

**Review Context:** Phase 1: Mill bootstrap + core jar production for IW-345 (Iteration 1/3)
**Files Reviewed:** 7 (build.mill, core/project.scala, mill, .mill-version, iw-run, .gitignore, .github/Dockerfile.ci)
**Skills Applied:** iterative-works:code-review-style, iterative-works:code-review-security
**Timestamp:** 2026-04-21 16:59
**Git Context:** git diff 7969f1fd1e9924c4b79d54273b3d500412279971

---

<review skill="code-review-style">

## Code Style Review

### Critical Issues

None found.

### Warnings

#### Temporal comment in `build.mill`
**Location:** `build.mill:31`
**Problem:** The inline comment `// core/test/** stays with scala-cli for Phase 1 (141 munit tests).` embeds temporal/historical context ("Phase 1") and a snapshot count ("141 munit tests") that will go stale.
**Impact:** Violates the rule that comments describe code as it is now, not its history or transient state.
**Recommendation:** Replace with an evergreen reason for the exclusion.

```scala
// Test sources are compiled by scala-cli; Mill packages compiled classes only.
```

#### Temporal comment in `build.mill` (second instance)
**Location:** `build.mill:24`
**Problem:** `// Dashboard-only deps — move to a dedicated dashboard module in Phase 2.` references a future phase name.
**Recommendation:** Reword to state the current structural fact without the phase reference.

```scala
// Dashboard-only deps; currently bundled here pending a dedicated dashboard module.
```

#### `DEFAULT_MILL_VERSION` in `./mill` wrapper is stale relative to `.mill-version`
**Location:** `mill:5`
**Problem:** The wrapper hard-codes `DEFAULT_MILL_VERSION="1.1.0-RC3"` while `.mill-version` pins `1.1.5`.
**Recommendation:** Add a brief comment noting that `.mill-version` always takes precedence and the fallback is an upstream default.

### Suggestions

- `./mill` missing PURPOSE header (vendored upstream; shebang-adjacent lines acceptable)
- SYNC comment placement asymmetry (project.scala header vs build.mill module body)
- Dockerfile has `# Node 20 - pre-downloaded for Phase 3 frontend pipeline` — same temporal-comment concern

</review>

---

<review skill="code-review-security">

## Security Review

### Critical Issues

None found.

### Warnings

#### No checksum verification for the Mill launcher download (`./mill` wrapper)
**Location:** `mill:154`
**Problem:** When the cached launcher is absent, the wrapper fetches the Mill binary over HTTPS and executes it without any integrity check.
**Mitigation posture:** `./mill` is a vendored canonical Mill upstream wrapper; diverging from upstream is a project decision.

#### `GITHUB_RELEASE_CDN` env var is trusted as a URL prefix without validation
**Location:** `mill:7,142`
**Problem:** Env var prepended to GitHub URL without validation.
**Mitigation posture:** Upstream pattern; mitigated if checksum is added.

#### Node 20 tarball installed from `deps/` without checksum verification in Dockerfile
**Location:** `.github/Dockerfile.ci:45-48`
**Problem:** `deps/node.tar.xz` is gitignored and trusted at image-build time.
**Mitigation posture:** Matches the pre-existing `deps/jdk.tar.gz` and `deps/scala-cli` pattern — out-of-scope for this phase.

### Suggestions

- Glob-based symlink `ln -s /opt/node-v20.*` (same as pre-existing jdk pattern)
- `MILL_OUTPUT_DIR` env var controls download path (vendored upstream behaviour)
- `iw-run` subshell `cd` pattern is safe

</review>

---

## Summary

- Critical issues: 0
- Warnings: 6 (3 style, 3 security)
- Suggestions: 6

## Resolution (Iteration 2)

Applied fixes:

- **`build.mill:24`** — replaced "move to a dedicated dashboard module in Phase 2" with "currently bundled here pending a dedicated dashboard module".
- **`build.mill:30-31`** — replaced "stays with scala-cli for Phase 1 (141 munit tests)" with "Test sources are compiled by scala-cli; Mill packages compiled classes only."
- **`.github/Dockerfile.ci:44`** — replaced "pre-downloaded for Phase 3 frontend pipeline" with "pre-downloaded for the dashboard frontend build pipeline".
- **`mill:2-8`** — added PURPOSE header and a clarifying comment that `DEFAULT_MILL_VERSION` is the upstream fallback while `.mill-version` is authoritative.

Post-fix: `./mill iwCoreJar` still builds cleanly. No functional changes.

Deferred (out of scope for this phase):

- Mill launcher download checksum verification (`./mill` is vendored upstream; would diverge from canonical wrapper).
- `GITHUB_RELEASE_CDN` HTTPS validation (same upstream-divergence concern).
- Node tarball checksum in Dockerfile (matches pre-existing `deps/jdk.tar.gz` / `deps/scala-cli` pattern; worth revisiting as a project-wide hardening pass).
