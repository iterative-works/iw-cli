# Code Review Results

**Review Context:** Phase 2: BuildToolCleanup built-in (Mill / Bloop / docker-compose) for issue IW-382 (Iteration 1/3)
**Files Reviewed:** 9
**Skills Applied:** architecture, scala3, composition, testing, security, style
**Timestamp:** 2026-06-14 19:31:10
**Git Context:** git diff 8835408

Files:
- core/model/BuildToolCleanup.scala (new)
- core/commands/BuildToolCleanupRunner.scala (new)
- core/test/BuildToolCleanupTest.scala (new)
- core/adapters/Process.scala
- core/commands/CommandEnv.scala
- core/commands/LiveCommandEnv.scala
- core/commands/Rm.scala
- core/test/RmHarnessTest.scala
- core/test/fixtures/FakeCommandEnv.scala

---

## Summary

- **Critical issues:** 2 (both from testing — Bloop harness coverage gap; warning-assertion brittleness)
- **Warnings:** ~10 (notable: unbounded per-tool subprocess timeout; missing Scaladoc; untested `timedOut`/`NonFatal` paths; enum-vs-boolean modeling)
- **Suggestions:** several (forward-looking refactors, mostly declined as YAGNI)

No architecture, security-injection, or style criticals. The FCIS boundary is intact:
`BuildToolCleanup.detect` is pure; `BuildToolCleanupRunner` does all I/O through
`env.fs`/`env.process`/`env.console`. No command-injection vector (all argv literal,
guarded by `commandExists` + `SafeCommandPattern`).

### Disposition (Iteration 2 actions)

**Fixed:**
1. **[critical] Bloop harness coverage** — added harness scenarios driving `Rm.run`
   with a `.bloop` marker (and a `.scala-build` variant) asserting `runIn` issues
   `bloop exit`. Covers the runner's OR-probe composition end-to-end.
2. **[security warning] Unbounded teardown timeout** — `runIn` now takes an explicit
   `timeoutMs`; the runner passes a bounded `TeardownTimeoutMs` (60s) so a wedged
   daemon cannot freeze `rm` for the 5-minute default. Directly implements the
   analysis "never block rm" contract.
3. **[testing] `timedOut` path** — added a harness scenario (`timedOut = true`,
   `exitCode = 0`) asserting a warning is emitted and removal proceeds.
4. **[testing] No `run` fallback** — happy-path scenario now also asserts the teardown
   commands did NOT go through `run` (`invocationList`), pinning the cwd-via-`runIn`
   contract (D1).
5. **[testing] Ordered teardown** — happy-path now asserts the full ordered `runInList`
   (mill before docker), not just `contains`.
6. **[security/scala3/arch] Subprocess output handling** — `linesIterator.nextOption()`
   instead of `.next()`, truncated to 200 chars, guarding against control-char/huge-line
   relay and removing the implicit non-empty precondition.
7. **[style] Scaladoc** — added on `BuildToolCleanup.detect` (incl. ordering guarantee),
   `Process.runIn` (the cwd contract), the `BuildToolCleanupRunner` object, and
   `@param` docs on `BuildToolProbe`/`BuildToolTeardown`.
8. **[style/arch] Comments** — replaced the opaque `D2` label with a self-contained
   comment; documented the intentional hooks-before-builtin order in `Rm`; noted the
   deliberately broad marker-gated `bloop exit` (analysis Decision D3).

**Declined (documented):**
- **`BuildToolProbe` → `Set[BuildTool]` enum** (scala3/composition warning): the boolean
  struct is clear for a fixed set of 3 tools, the pure `detect` tests are exhaustive, and
  `tool` still needs the executable-name string for `commandExists`. Refactor is taste,
  not correctness — YAGNI until a 4th tool appears.
- **`.scala-build` as a Bloop trigger** (architecture suggestion): intentional per analysis
  Decision D3 — marker-gated unconditional `bloop exit`, gated on `commandExists("bloop")`
  so scala-cli-only setups silently skip. Now documented in a code comment.
- **`NonFatal`-throw harness test** (testing warning): would require new throw plumbing in
  `FakeProcess`; the catch is trivial and low-risk. Deferred.
- **Per-`cwd` `scriptResponse` keying** (testing/composition suggestion): no current test
  needs per-directory output scripting. Forward work.

---

## Reviews

### architecture — 0 critical, 2 warnings
- Bloop two-marker decision lives inline in the runner constructor (not a named/tested
  unit). → addressed by the new Bloop harness scenarios + a code comment.
- Hooks-vs-builtin order undocumented. → added an explanatory comment in `Rm`.

### scala3 — 0 critical, 2 warnings
- `BuildToolProbe` boolean struct vs `enum`/`Set[BuildTool]` (cf. `BuildSystem`). → declined (YAGNI).
- `Option(result.stderr)` null-guard idiom unnecessary (fields are non-null `String`). → cleaned up.

### composition — 0 critical, 2 warnings
- `Process` growing overloads (`run`/`runIn`); `runOne` fuses announce+execute. → accepted as
  pragmatic for the current size; not refactored.

### testing — 2 critical, 4 warnings
- Bloop path uncovered at harness level. → FIXED.
- Warning assertion fragile vs `description`. → softened (assert Warning-line presence +
  removal proceeds, alongside wording).
- `timedOut` / `NonFatal` paths untested. → `timedOut` FIXED; `NonFatal` deferred.
- No assertion teardown avoided `run`. → FIXED.

### security — 0 critical, 2 warnings
- Unbounded 5-min per-tool timeout can block `rm`. → FIXED (bounded `TeardownTimeoutMs`).
- Verbatim subprocess output line in warnings. → FIXED (nextOption + 200-char truncation).
- No injection vector (argv literal, `commandExists` + `SafeCommandPattern`). ✓

### style — 0 critical, 4 warnings
- Missing Scaladoc on new public API; opaque `D2` comment label. → FIXED.
