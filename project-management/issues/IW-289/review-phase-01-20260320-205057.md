# Code Review Results

**Review Context:** Phase 1: Pure decision logic for CI check results for issue IW-289 (Iteration 1/3)
**Files Reviewed:** 2
**Skills Applied:** code-review-style, code-review-scala3, code-review-testing, code-review-composition
**Timestamp:** 2026-03-20 20:50:57
**Git Context:** git diff c6ddce8

---

## Style Review

### Critical Issues
None found.

### Warnings

#### Test File Package Does Not Match Model Package
**Location:** `.iw/core/test/PhaseMergeTest.scala:4`
**Problem:** Test declares `package iw.core.application`, but code under test is in `iw.core.model`.
**Note:** `BatchImplementTest.scala` also uses `iw.core.application` — this is the established convention for model tests in this project.

#### `CIVerdict.TimedOut` Declared but Never Produced
**Location:** `.iw/core/model/PhaseMerge.scala:27`
**Problem:** `TimedOut` exists in enum but `evaluateChecks` never produces it.
**Note:** Intentional — designed for Phase 4 (timeout polling). Needs comment.

### Suggestions
- Consolidate imports in test file
- Rephrase `shouldRetry` docstring to imperative voice

---

## Scala 3 Review

### Critical Issues
None found.

### Warnings

#### `CIVerdict.SomeFailed` Parametric Case in Enum
**Location:** `.iw/core/model/PhaseMerge.scala:24`
**Problem:** Enum with parametric case breaks `CIVerdict.values`.
**Recommendation:** Consider sealed trait hierarchy.

### Suggestions
- Add `show` extension for CICheckStatus display text
- Test package mismatch (duplicate of style review)

---

## Testing Review

### Critical Issues
None found.

### Warnings

#### Low-Signal Constructor Tests
**Location:** `.iw/core/test/PhaseMergeTest.scala:24-37`
**Problem:** Tests for case class construction and enum value counting test the compiler, not logic.

#### `buildRecoveryPrompt` Contract Not Fully Pinned
**Location:** `.iw/core/test/PhaseMergeTest.scala:169-198`
**Problem:** Tests use `contains()` but don't verify exact output format.
**Recommendation:** Add one exact-output test.

### Suggestions
- Test empty list case for `buildRecoveryPrompt`
- Rename Unknown test to signal design intent

---

## Composition Review

### Critical Issues
None found.

### Warnings

#### `evaluateChecks` Double Traversal
**Location:** `.iw/core/model/PhaseMerge.scala:54-59`
**Problem:** Two passes (filter + exists) where partition would express intent better.
**Recommendation:** Use `partition` then check `rest.exists`.

#### `shouldRetry` Takes Full Config
**Location:** `.iw/core/model/PhaseMerge.scala:67-68`
**Problem:** Only uses `maxRetries` but receives entire `PhaseMergeConfig`.

### Suggestions
- Add `isTerminalFailure` method on `CICheckStatus`
- Inline `lines` val in `buildRecoveryPrompt`

---

## Summary

- **Critical issues:** 0
- **Warnings:** 5
- **Suggestions:** 8

No blocking issues. Warnings are valid improvements, particularly the sealed trait suggestion and partition refactoring.
