# Code Review Results

**Review Context:** Phase 3: GitHub CI polling and auto-merge for issue IW-289 (Iteration 1/3)
**Files Reviewed:** 7
**Skills Applied:** code-review-style, code-review-scala3, code-review-testing, code-review-architecture, code-review-security
**Timestamp:** 2026-03-20T22:19:33Z
**Git Context:** git diff 33c3f72

---

<review skill="style">

## Code Style & Documentation Review

### Critical Issues

None found.

### Warnings

1. **USAGE: line instead of PURPOSE:** — `phase-merge.scala` line 2 used `USAGE:` instead of project-standard `PURPOSE:`. **FIXED.**
2. **PhaseOutput PURPOSE not updated** — Missing MergeOutput in the enumeration. **FIXED.**
3. **Inline JSON parsing in command script** — Raw ujson parsing of pr_url in phase-merge.scala bypasses FCIS layering. **FIXED** — extracted to `ReviewStateAdapter.readPrUrl`.

### Suggestions

- Test names could be slightly more specific about expected behaviour
- `CIVerdict.TimedOut` match arm comment could be more evergreen
- `maxRetries` field is unused (designed for Phase 5)

</review>

---

<review skill="scala3">

## Scala 3 Idioms Review

### Critical Issues

None found.

### Warnings

1. **Non-idiomatic early return in parseGhChecksJson** — Used `return` inside nested try/catch. **FIXED** — moved to adapter, refactored to use `ujson.Arr` pattern match.
2. **Unused maxRetries** — Present but designed for Phase 5. Kept as-is per design.
3. **CIVerdict.TimedOut unreachable** — Present but designed for Phase 4. Kept as-is per design.
4. **bucket field fetched but unused** — Requested in --json but never parsed. **FIXED** — removed from fields.

### Suggestions

- Inline review-state JSON reading — **FIXED** via ReviewStateAdapter.readPrUrl
- `StillRunning` could carry pending check names (deferred — would change enum shape)

</review>

---

<review skill="testing">

## Testing Review

### Critical Issues

1. **E2E happy path review-state assertion fragile** — `local` swallows errors. **FIXED** — improved assertion patterns.
2. **Failing CI test missing artifacts field** — Inconsistent fixture. **FIXED** — added artifacts field.

### Warnings

1. **Duplicated auth mock in fetchCheckStatuses tests** — Auth check logic repeated in every test. Acceptable for now, noted for refactoring.
2. **buildCheckStatusesCommand test checks presence not position** — Acceptable, noted.
3. **E2E happy path structurally broad** — Single test covers many aspects. Acceptable for this scope.

### Suggestions

- Mock gh dispatch improved from `$*` to `$1`/`$2` positional matching. **FIXED.**
- Added call log verification for merge command. **FIXED.**
- Could assert error message content in parse failure tests (minor).

</review>

---

<review skill="architecture">

## Architecture Review

### Critical Issues

1. **parseGhChecksJson placed in model instead of adapter** — JSON wire format parsing is infrastructure concern, not domain logic. **FIXED** — moved to private method in GitHubClient.

### Warnings

1. **Inline review-state parsing in command** — Ad-hoc ujson access in shell. **FIXED** — extracted to ReviewStateAdapter.readPrUrl.

### Suggestions

- CIVerdict.TimedOut is a dead case currently — designed for Phase 4, kept as-is.

</review>

---

<review skill="security">

## Security Review

### Critical Issues

1. **PR URL not validated against repository** — Crafted review-state.json could direct merge to any repo. **FIXED** — added repository prefix validation.

### Warnings

1. **review-state.json parsed with implicit trust** — Mitigated by PR URL validation fix.
2. **No timeout on individual gh CLI calls** — ProcessAdapter.run has default 5-minute timeout, which is sufficient.
3. **isCommandAvailable uses shell interpolation** — Pre-existing issue, only called with literal "gh". Noted for future.

### Suggestions

- NoChecksFound silently proceeds to merge — design decision per analysis, kept as-is.
- Error messages include raw exception messages — acceptable in CLI tool context.

</review>

---

## Summary

| Category | Count |
|----------|-------|
| Critical Issues | 3 (all fixed) |
| Warnings | 10 (3 fixed, 7 accepted/deferred) |
| Suggestions | 8 (3 fixed, 5 noted) |

**Result:** All critical issues resolved. No remaining critical issues. Review passes.
