# Code Review Results

**Review Context:** Phase 2: PR number extraction from review-state for issue IW-289 (Iteration 1/3)
**Files Reviewed:** 2
**Skills Applied:** code-review-style, code-review-testing, code-review-scala3
**Timestamp:** 2026-03-20T21:41:21Z
**Git Context:** git diff e151394060fb8da8c084234cb1f73bd8c434b068

---

## Style Review

### Critical Issues
None.

### Warnings
- Error case tests used manual pattern matching instead of `assertEquals` or `assert(result.isLeft)` — **FIXED**: Updated to use `assertEquals` and `assert(result.isLeft)` consistently.
- `extractPrNumber` name vs "PR/MR" in docs — accepted: PR is the dominant term, function handles both.

### Suggestions
- Section comment casing consistency — accepted as-is (proper nouns capitalized, generic terms lowercase).
- `// expected` comments on Left branches — **FIXED**: Removed by switching to `assertEquals`.

---

## Testing Review

### Critical Issues
None.

### Warnings
- Regex boundary gap (no end anchor) — **INVESTIGATED**: Scala regex `unapply` does full-string matching; URLs with trailing paths are already correctly rejected. Verified with runtime test.
- Leading whitespace not tested — **FIXED**: Added test for leading whitespace.
- Error assertions don't check message content for blank inputs — **FIXED**: Now uses `assertEquals` with exact expected message.

### Suggestions
- Two GitHub tests cover similar paths — kept both (one with real org URL serves as documentation).
- No test for PR number overflow — accepted: PR numbers won't approach Int.MaxValue in practice.

---

## Scala 3 Review

### Critical Issues
None.

### Warnings
- Manual `fail` instead of `assertEquals` — **FIXED** (same as style warning).
- Regex patterns and query string/fragment handling — **INVESTIGATED**: Full-string matching means fragments/queries already rejected. Added note to scaladoc would be nice but not blocking.

### Suggestions
- Use named regex binding (`number` instead of `n`) — minor, accepted as-is for consistency with codebase.
- `n.toInt` unchecked for overflow — no action needed.

---

## Summary

- **Critical issues:** 0
- **Warnings:** 6 total (3 fixed, 2 investigated and found non-issues, 1 accepted)
- **Suggestions:** 5 total (all accepted or acknowledged)
- **Verdict:** PASS — no critical issues, all substantive warnings addressed
